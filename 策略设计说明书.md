# 策略0717 — RSI Sentinel 完整升级方案

> 设计时间: 2026-07-17
> 基于 RSI Sentinel v5.0 的全面升级，融合传统规则 + 多模型委员会 + ATR动态调整

---

## 一、核心设计理念

```
不再是一套信号吃所有行情
而是: 判断当前市场状态 → 选择对应子策略 → 多位助手共同决策 → 你做最终决定
```

### 三个关键转变

| 旧方案 (v5.0) | 新方案 (策略0717) | 为什么 |
|:-------------|:-----------------|:-------|
| 固定信号优先级 | 按市场状态动态选择 | cross_70 在牛市中应该是好信号，熊市中就是陷阱 |
| 固定仓位 1% | 信号质量 × 大盘 × 波动率 × ATR 动态仓位 | 每只股票、每个信号都不同，不该一个仓位吃遍天 |
| 单一退出机制 | 5层退出 + ATR辅助 + 背离修正 | 不同原因造成的下跌需要用不同方法应对 |

---

## 二、目录结构 — 交易员视角

```
策略0717/
│
├── 1_策略配置/              ★ 你唯一需要修改的地方
│   ├── 买入信号配置.yaml      # 什么条件下买入
│   ├── 卖出规则配置.yaml      # 什么条件下卖出（含ATR）
│   ├── 仓位配置.yaml         # 买多少（含大盘/波动率调整）
│   └── 参数配置.yaml         # RSI周期/ATR倍数等所有数值
│
├── 2_策略引擎/              # 只写一次，不用再碰
│   ├── 规则执行器.py          # 读取 yaml → 执行交易规则
│   ├── 计算指标.py            # RSI/RSI_MA/ATR/背离检测
│   └── 因子评分器.py          # 信号质量打分 + 委员会投票
│
├── 3_数据/                  # 数据加载和存储
│   ├── 数据加载器.py
│   └── raw/                 # K线数据目录
│
├── 4_回测系统/
│   ├── 回测引擎.py           # 事件驱动逐K线
│   └── 生成报告.py           # 产出：回测报告.html
│
├── 5_ML训练工厂/             ★ 模型训练车间
│   ├── 训练配置.yaml         # 选择模型/目标/数据范围
│   ├── 一键训练.py           # 跑这个就行
│   └── 已训练模型/           # 训练好的模型文件
│
├── 6_实盘系统/              # 后续阶段
│   ├── 交易客户端.py          # 连接券商
│   ├── 运行引擎.py            # 实时运行
│   └── 监控面板.py            # 实时看信号/持仓
│
├── 7_输出/                  ★ 你每次看的东西
│   ├── 回测报告.html          # 首页：绩效 + 配置变更
│   ├── K线回放报告.html       # 逐笔看图验证
│   ├── 实验对比.html          # 两次实验参数/绩效对比
│   ├── 因子仪表盘.html        # 每个因子/模型贡献度
│   ├── 背离分析.html          # 顶/底背离识别准确率
│   └── ML表现看板.html        # 模型最近准不准
│
├── 8_实验记录/              ★ 每次回测自动存档
│   ├── 2026-07-17_实验1_RSI上穿20/
│   │   ├── 本次配置.yaml      # 这次用的配置
│   │   ├── 回测报告.html
│   │   └── 交易明细.csv
│   └── 2026-07-18_实验2_加了ATR/
│       └── ...
│
├── hs300_list.txt           # HS300成分股列表
└── 启动说明.md              # 怎么用
```

---

## 三、市场状态分类器 — 4种模式

### 判断指标

只用 HS300 指数，每根K线判断一次：

| 状态 | 条件 | 应对策略 |
|:----|:----|:---------|
| **进攻模式** 🟢 | HS300 > 120日线 + 20日线斜率 > 0 | 目标仓位 80-98%，所有买入信号正常执行 |
| **震荡模式** 🟡 | 不上不下，不满足其他条件 | 目标仓位 30-50%，买入信号打 5-7 折 |
| **防守模式** 🔴 | HS300 < 120日线 + 20日线斜率 < 0 | 目标仓位 10-20%，仅 cross_20 可买 |
| **收缩模式** ⚠️ | 市场平均ATR > 前20日ATR × 1.5 | 收紧所有止损/止盈，降低基准仓位 |

### 状态切换的影响

| 参数 | 进攻 | 震荡 | 防守 | 收缩 |
|:----|:----|:----|:----|:----|
| 目标总仓位 | 98% | 50% | 20% | 30% |
| 信号质量折扣 | 1.0 | 0.6 | 0.3 | 0.5 |
| 硬止损 | 4×ATR | 3×ATR | 2×ATR | 2×ATR |
| 分批止盈第一目标 | 10% | 5% | 3% | 5% |
| RSI峰值回落阈值 | 20点 | 15点 | 10点 | 10点 |

---

## 四、买入委员会 — 4+1 位助手

### 委员会成员

| 成员 | 职责 | 最看重的因素 |
|:----|:----|:------------|
| **传统规则** | RSI上穿20/30/MA/70 基础判断 | RSI值、上穿形态 |
| **助手A — LightGBM** | 捕捉局部异常细节 | RSI上升速度、成交量突变 |
| **助手B — XGBoost** | 判断整体趋势是否支持 | 趋势确认、支撑阻力位 |
| **助手C — RandomForest** | 评估大盘和板块环境 | 大盘趋势、板块动量 |
| **助手D — ATR专家** | 判断突破力度 | 价格波动 vs ATR |

### 买入流程

```
每根K线扫描每只股票:

  ① 传统规则检测 RSI上穿
     ├── 上穿20 → 基础分 1.0
     ├── 上穿30 → 基础分 0.7
     ├── 上穿MA → 基础分 0.4
     └── 上穿70 → 基础分 0.2

  ② 背离检查
     ├── 底背离 → 基础分 × 1.2
     ├── 顶背离 → 基础分 × 0.3（仅限 cross_20/30）
     └── cross_70 + 顶背离 → ❌ 禁止买入

  ③ 助手投票 (各输出 0~1 分)
     ├── LightGBM: 假突破概率 → 基础分 × (1-假突破概率)
     ├── XGBoost:  趋势支持度
     ├── RF:       大盘环境支持度
     └── ATR专家:  突破幅度 / (2×ATR)

  ④ 综合评分
     最终分 = 基础分 × 各助手平均分 × 大盘系数

  ⑤ 仓位计算
     最终持仓 = 基准仓位(20万) × 最终分
     上限 = min(最终持仓, 单只上限10%, 剩余现金)
```

### 投票决策

| 委员会投票 | 你的操作建议 |
|:----------|:------------|
| 4-5人同意 | 正常买入 |
| 3人同意 | 减半仓买入 |
| 2人同意 | 再观察，不急于动手 |
| 1人同意以下 | 不买，等待更强信号 |

---

## 五、退出体系 — 5层保护 + ATR加持

### 5层退出

```
每根K线检查每只持仓，按顺序检查，任一触发则执行:

① 分批止盈
   ├── 盈利 > 5%  → 卖 1/3
   ├── 盈利 > 10% → 再卖 1/3
   └── 盈利 > 20% → 全清

② 硬止损
   ├── 亏损 > 3×ATR → 全卖
   └── 防守模式下收紧到 2×ATR

③ RSI站岗价
   ├── RSI下穿80/70/MA → 设站岗价 = 最低价
   └── 价格跌破 站岗价 - 0.5×ATR → 卖出 (ATR缓冲防假跌破)

④ 动能衰竭退出 (解决69.9问题)
   ├── 记录买入后RSI最高峰值
   ├── 回落阈值 = 15 × (股票ATR / 平均ATR)
   └── RSI从峰值回落超过阈值 → 卖出

⑤ 时间退出
   ├── 持仓 ≥ 80根K线
   └── 亏损 > 8% 或 不赚钱 → 清仓
```

### 退出修正信号

```
卖出时检查以下修正信号:

  底背离 → 暂缓卖出，再观察 2-3 根K线
  顶背离 → 加速卖出，站岗价加严

  大盘连续跌 3天累计 -3% → 亏损持仓减半
  大盘连续跌 5天累计 -5% → 暂停买入，总仓减至70%
  大盘连续跌 10天累计 -10% → 强制减仓至50%
```

---

## 六、ATR 的 4 个具体用法

### 用法1：RSI峰值回落阈值调整

```
回落阈值 = 15 × (该股ATR / 全市场平均ATR)

示例:
  全市场平均 ATR = 0.5
  贵州茅台 ATR = 3.5 → 阈值 = 15 × (3.5/0.5) = 105
  小盘股 ATR = 0.2  → 阈值 = 15 × (0.2/0.5) = 6

茅台RSI要掉105点才触发 → 难触发（茅台稳，少卖飞）
小盘股掉6点就触发 → 敏感（波动小，防深跌）
```

### 用法2：站岗价缓冲带

```
当前: 价格 < 站岗价 → 立刻卖
改良: 价格 < 站岗价 - 0.5×ATR → 才卖

茅台 ATR=3.5 → 缓冲 1.75 → 毛刺K线不触发
小盘 ATR=0.2 → 缓冲 0.1 → 毛刺也过不去，但真跌马上触发
```

### 用法3：硬止损宽度

```
止损 = 3 × ATR

茅台 ATR=3.5 → 止损 10.5元（价格1500 → 比例 0.7%，宽松合理）
小盘 ATR=0.2 → 止损 0.6元（价格8元 → 比例 7.5%，避免死扛）
```

### 用法4：突破真伪判断

```
买入时:
  RSI上穿 + 价格移动 > 2×ATR → 真突破，信号加强 (×1.2)
  RSI上穿 + 价格移动 < 0.5×ATR → 假突破，信号减弱 (×0.5)

卖出时:
  跌幅 > 2×ATR → 异常下跌，加速卖出
  跌幅 < 0.5×ATR → 正常波动，不急于动
```

---

## 七、ML训练工厂

### 当前提供 4 个模型

| 模型 | 擅长 | 适合预测 | 数据需求 |
|:----|:----|:--------|:--------|
| **LightGBM** | 快、吃局部特征 | 买入假突破、短暂异常 | 少（千级） |
| **XGBoost** | 稳健、防过拟合 | 趋势判断、整体方向 | 中（万级） |
| **RandomForest** | 抗噪声、全面投票 | 大盘环境、板块选择 | 中 |
| **Ensemble** | 结合三者优点 | 最终决策参考 | 多（百万级） |

### 训练流程

```
训练配置.yaml (选模型/选数据/设参数)
       ↓
 一键训练.py (自动训练+调参+保存)
       ↓
 模型评估报告.html (AUC/准确率/最重要的特征)
       ↓
 你决定: 启用 或 关闭
       ↓
 因子配置.yaml → 启用该模型
       ↓
 回测验证: 有ML vs 无ML 对比
```

### 委员会训练策略

```
每只模型独立训练:
  LightGBM: 训练目标 = "买入后20根K线涨了还是跌了"
  XGBoost:  训练目标 = "当前趋势是否支持持有"
  RF:       训练目标 = "大盘状态 + 板块效应"
  ATR专家:  不需要训练，规则驱动

训练后:
  各自输出 0~1 分
  加权平均 = 最终委员会分
  权重在实验对比中自动优化
```

---

## 八、回测看板

### 首页 — 绩效总览

```
╔══════════════════════════════════════════════════════════╗
║  策略: RSI Sentinel 增强版   实验: 2026-07-17_实验3      ║
╠══════════════════════════════════════════════════════════╣
║                                                        ║
║  绩效              │ 与上次比较                          ║
║  收益率: +XX%      │ 上次: +YY%  ▲ / ▼                  ║
║  最大回撤: XX%     │ 上次: YY%                          ║
║  胜率: XX%         │ 上次: YY%                          ║
║  交易次数: XXXX    │                                    ║
╠══════════════════════════════════════════════════════════╣
║  本次配置变更:                                        ║
║  │ 📌 新增 ATR: 站岗价缓冲 0.5×ATR                    ║
║  │ 📌 启用 XGBoost 委员                                ║
║  │ 📌 RSI峰值回落阈值: 固定15→ATR动态                 ║
╠══════════════════════════════════════════════════════════╣
║  [逐笔K线回放] [实验对比] [因子仪表盘] [背离分析] [ML报告]  ║
╚══════════════════════════════════════════════════════════╝
```

### K线回放 — 逐笔验证

```
股票: 600519  时间段: 2025-03  (共67笔交易)

     ┌───────────────────────────────────┐
     │          K线图                     │
     │                                    │
     │  🟢买入(底部)  🟡卖1/3(半山腰)   │
     │  ↑              ↕                 │
     │  │    ────     │                  │
     │  │   /    \    │                  │
     │  │  /      \   │                  │
     │  │ /        \  ↕                 │
     │  │/          \ 🔴最终卖出         │
     └───────────────────────────────────┘

  买点: 2025-03-15 09:30
  买入原因: RSI上穿20  大盘: 📈上涨
  信号质量: 0.85  (底背离确认 + 成交量放大)
  仓位: 17万 (基准20万 × 质量0.85)
  委员会: ✅ 全票通过 (LightGBM 82% / XGBoost 76% / RF 71% / ATR 突破确认)

  卖点: 2025-03-28 10:30
  卖出原因: ATR站岗价缓冲跌破 (真跌确认)
  卖出详情: 第1次卖1/3(5%), 第2次卖1/3(10%), 最终卖出(站岗价触发)
  总盈亏: +14.2%

  ◀ 上一笔        [第34笔/共67笔]        下一笔 ▶
```

### 实验对比 — 改了什么一目了然

```
╔════════════════════════════╦═══════════════════════════════╗
║  实验2: 无ATR              ║  实验3: 加ATR                 ║
╠════════════════════════════╬═══════════════════════════════╣
║  参数差异                    ║                               ║
║  RSI回落阈值: 固定15       ║  回落阈值: ATR动态(6~105)      ║
║  硬止损: 关闭              ║  硬止损: 3×ATR                ║
║  站岗价: 无缓冲             ║  站岗价: 0.5×ATR缓冲           ║
╠════════════════════════════╬═══════════════════════════════╣
║  绩效对比                    ║                               ║
║  收益率: +XX%              ║  收益率: +XX% ▲/▼             ║
║  最大回撤: XX%             ║  最大回撤: XX% ▲/▼             ║
║  胜率: XX%                 ║  胜率: XX% ▲/▼                ║
╠════════════════════════════╬═══════════════════════════════╣
║  K线图对比                   ║                               ║
║  ┌──────────────────┐     ║  ┌──────────────────┐         ║
║  │  毛刺K线导致误卖  │     ║  │  正确识别，没卖   │         ║
║  └──────────────────┘     ║  └──────────────────┘         ║
╠════════════════════════════╬═══════════════════════════════╣
║  🎯 结论: ATR加入后减少了毛刺误卖，但卖点略低              ║
╚════════════════════════════╩═══════════════════════════════╝
```

---

## 九、回测 vs 实盘的关系

```
你改 策略配置/*.yaml
       ↓
  跑回测 → 看图验证 ✅
       ↓
  满意了？
  ├── 是 → 实盘系统直接用同一套配置启动
  └── 否 → 继续改配置 → 再跑

回测和实盘调用同一个规则执行器.py
读同一套 yaml 配置
回测赚钱的配置，实盘不会变形
```

### 实盘三阶段

| 阶段 | 做法 | 适合时间 |
|:----|:----|:--------|
| 提醒模式 | 系统弹信号，你手动下单 | 1-3个月 |
| 半自动 | 系统自动下单，你有一键撤销权 | 3-6个月 |
| 全自动 | 系统全自动，仅异常时通知你 | 成熟后 |

---

## 十、落地路线图

### 第一批 (1-2天) — 跑通单股回测，看图验证

- [ ] 建目录结构 + yaml 配置文件
- [ ] 规则执行器（读取 yaml → 执行买卖规则）
- [ ] 回测引擎（事件驱动逐K线）
- [ ] K线回放报告.html（逐笔看图）
- [ ] 能用 1-2 只股票验证策略是否按预期运行

### 第二批 (3-5天) — 完成策略框架

- [ ] 大盘状态分类器
- [ ] 仓位管理引擎（质量×大盘×波动率）
- [ ] ATR 4个用法全部实现
- [ ] 退出体系（5层 + 背离修正）
- [ ] 实验对比报告.html
- [ ] 能跑全量 HS300 回测

### 第三批 (1周后) — ML增强

- [ ] 训练工厂（一键训练 + 模型评估）
- [ ] LightGBM + XGBoost + RF 三模型训练
- [ ] 委员会投票机制集成
- [ ] 有 ML vs 无 ML 对比报告
- [ ] 因子仪表盘.html（每个助手贡献可视化）

---

## 十一、你以后的工作方式

| 日常操作 | 怎么做 | 难度 |
|:--------|:------|:----|
| 改RSI阈值 20→25 | 改 yaml 里的数字 | ★☆☆☆☆ |
| 开关一个买入信号 | yaml 写 true/false | ★☆☆☆☆ |
| 调整ATR缓冲倍数 | 改数字 0.5 → 0.8 | ★☆☆☆☆ |
| 对比两次实验不同在哪 | 自动生成对比报告 | ★☆☆☆☆ |
| 逐笔看K线验证 | 打开网页点下一笔 | ★☆☆☆☆ |
| 训练新模型 | 改训练配置yaml → 一键训练 | ★★☆☆☆ |
| 加一个全新指标 | 告诉我公式 | ★★★☆☆ (我来写) |

**核心原则：你改配置、看图、做决策。我写引擎、训练模型、生成报告。**

---

## 附录A：ML训练任务全景 (完整版)

> 本策略所有判断环节均采用 ML 训练替代人工规则。
> 唯一保留的传统规则只做基础信号检测（RSI上穿20/30/MA/70），
> 所有"真伪判断"全部由 ML 完成。

### A1. ML 任务总览

| 编号 | ML任务 | 解决的问题 | 判断内容 | 数据来源 | 优先级 |
|:----|:-------|:----------|:---------|:--------|:------|
| **T1** | **买入假突破判断** | 信号是真的要涨还是假突破 | 买入信号发生后20根K线是否真涨 | 历史所有买入信号 + 后续走势 | P0 ✅ 已有数据 AUC=0.8864 |
| **T2** | **RSI_MA盘整判断** | MA上升是盘整噪声还是真突破 | MA(current)>MA(prev)是盘整还是趋势启动 | 历史所有cross_MA信号 + 后续MA走势 | P0 🆕 新增 |
| **T3** | **入场时机判断** | 大阳线踏空 / 小阳线接盘 | 开盘买 / 突破买 / 不买 三选一 | 历史所有买入信号的入场情况 | P1 |
| **T4** | **RSI下穿真伪判断** | 下穿80/70/MA是真跌还是假摔 | 卖出后20根K线是否真跌 | 历史所有RSI下穿事件 | P1 |
| **T5** | **RSI动能衰竭判断** | 69.9到不了70，RSI从峰值回落 | 回落是趋势反转还是暂时回调 | 历史所有RSI峰值回落事件 | P2 |
| **T6** | **背离真伪判断** | 顶底背离是真的趋势反转还是假信号 | 背离发生后20根K线的方向 | 历史所有背离事件 | P2 |

### A2. 委员会成员（最终版）

经过所有讨论，买入委员会共有 **6 位成员**：

| 成员 | 类型 | 职责 |
|:----|:----|:-----|
| **传统规则** | 规则驱动 | 检测基础信号（RSI上穿20/30/MA/70） |
| **助手A — LightGBM** | ML (T1) | 买入假突破判断 |
| **助手B — RSI_MA盘整判断器** | ML (T2) | MA上升是真的吗 |
| **助手C — XGBoost** | ML | 趋势支持度判断 |
| **助手D — RandomForest** | ML | 大盘/板块环境判断 |
| **助手E — ATR专家** | 规则驱动 | 突破力度判断 |
| **助手F — 入场时机官** | ML (T3) | 什么时候买、怎么买 |

> T4/T5/T6 用于卖出端，不在买入委员会中。

### A3. 训练数据管理

所有 ML 任务共用一套历史回测数据，但每个任务提取不同的标签：

```
历史K线数据
    │
    ├── 提取所有买入信号 → T1买入假突破标签
    ├── 提取所有cross_MA信号 → T2盘整判断标签
    ├── 提取所有入场位置 → T3入场时机标签
    ├── 提取所有RSI下穿 → T4假跌判断标签
    ├── 提取所有RSI峰值 → T5动能衰竭标签
    └── 提取所有背离事件 → T6背离真伪标签
```

每个任务独立训练、独立启用/关闭，都在配置文件中控制。

### A4. 训练流程（不改代码）

```
① 打开 5_ML训练工厂/训练配置.yaml
② 选择要训练的任务 (T1~T6)
③ 选择模型 (LightGBM / XGBoost / RF / Ensemble)
④ 运行 python 5_ML训练工厂/一键训练.py
⑤ 查看 模型评估报告.html
⑥ 满意 → 在因子配置.yaml 中启用该模型
⑦ 不满意 → 调整参数重新训练
```

---

## 附录B：落地路线图（细化版）

### 落地原则

```
一只股票 → 跑通 → 看图验证 → 满意 → 加股票 → 再加...
一只股票 → 跑通 → 看图验证 → 不满意 → 改配置 → 再跑
```

**一次只上一只股票。单股跑通了，再加到十只、一百只、全 HS300。**
每只股票独立回测、独立出报告。你一只一只验证，全部验证通过再合起来跑组合。

---

### 第一批：单股回测 + K线回放报告（1-2天）

目标: 选一只股票，跑通完整的回测流程，看到带所有标记的 K 线图

交付物:
```
策略0717/
├── 1_策略配置/
│   ├── 买入信号配置.yaml
│   ├── 卖出规则配置.yaml
│   ├── 仓位配置.yaml
│   └── 参数配置.yaml
│
├── 2_策略引擎/
│   ├── 规则执行器.py        # 读取 yaml → 执行交易
│   ├── 计算指标.py          # RSI/RSI_MA/ATR/背离
│   └── 因子评分器.py        # 信号质量打分
│
├── 3_数据/
│   └── 数据加载器.py        # 加载单只股票 K 线数据
│
├── 4_回测系统/
│   ├── 回测引擎.py          # 单股事件驱动逐K线
│   └── 生成报告.py          # 产出 K线回放.html
│
├── 7_输出/
│   └── K线回放.html         # ★ 你双击打开看的东西
│
├── 8_实验记录/              # 每次跑自动存档
│
└── 启动说明.md
```

K线回放报告包含:
- 前复权K线蜡烛图（日期/开盘/收盘/最高/最低）
- RSI 副图指标线
- 买入标记（标注不复权买入价 + 哨兵价水平线）
- 卖出标记（标注不复权卖出价 + 站岗价/ATR缓冲水平线）
- 买入→卖出连线（从买入K线位置到卖出K线位置）
- 每笔交易的完整信息面板
- 上一笔/下一笔翻页
- 缩放功能

验证标准:
- [ ] 单只股票能跑完回测不报错
- [ ] 生成的 HTML 能双击打开，不需要服务器
- [ ] K线图显示正确，买入卖出标记在正确位置
- [ ] 哨兵价和站岗价水平线可见
- [ ] 连线从买入K线连到卖出K线
- [ ] 鼠标悬停显示所有价格
- [ ] 逐笔翻页正常

---

### 第二批：扩展到多只股票 + 完整策略框架（3-5天）

在第一批基础上加:
- 大盘状态分类器
- 仓位管理引擎（信号质量×大盘×波动率）
- ATR全部4个用法集成
- 完整退出体系（5层 + 背离修正）
- 多只股票批量回测
- 实验对比报告.html

---

### 第三批：ML增强（1周后）

- 训练工厂 + 一键训练
- T1买入假突破 (已有数据)
- T2 RSI_MA盘整判断
- T3 入场时机判断
- 委员会投票集成
- 有ML vs 无ML对比报告
- 因子仪表盘

---

## 附录C：模块化独立程序结构 + 全中文命名规范

### 设计原则

```
① 每一个策略环节 = 一个独立文件
   数据加载、某个买入条件、某个卖出条件、仓位计算...
   每个文件只做一件事，你一看文件名就知道它管什么

② 全中文命名
   文件夹名、文件名、函数名、变量名、注释
   全部用中文，不出现英文标识符

③ 改了什么 = 改了哪个文件
   每次修改后，我告诉你: "改动了 5_买入规则/RSI上穿20.py"
   你打开 Obsidian 看项目跟踪，就知道完成到什么程度
```

### 第一批使用的完整目录

```
策略0717/
│
├── 1_策略配置/              ★ 你改数字的地方
│   ├── 买入信号配置.yaml
│   ├── 卖出规则配置.yaml
│   ├── 仓位配置.yaml
│   └── 参数配置.yaml
│
├── 2_策略引擎/              ★ 计算模块，每项一个文件
│   ├── 计算RSI.py            # def 计算RSI(价格列表, 周期=14)
│   ├── 计算RSI均线.py        # def 计算RSI均线(RSI列表, 周期=20)
│   ├── 计算ATR.py            # def 计算ATR(K线数据, 周期=14)
│   ├── 检测背离.py           # def 检测顶背离(价格, RSI) / def 检测底背离(...)
│   ├── 检测大盘状态.py       # def 判断大盘状态(大盘K线)
│   ├── 信号质量评分.py       # def 评分(信号类型, 各项特征)
│   ├── 读取规则执行买卖.py   # 读取yaml → 执行买入/卖出/仓位规则
│   └── 交易记录器.py         # 记录每根K线的持仓状态
│
├── 3_数据/
│   ├── 加载单只股票.py       # def 加载股票(股票代码, 数据路径)
│   └── 加载大盘数据.py       # def 加载大盘指数()
│
├── 4_回测系统/
│   ├── 单股回测引擎.py       # 逐K线跑一只股票
│   ├── 生成K线回放报告.py    # 产出 → 9_输出/K线回放.html
│   └── 生成交易明细CSV.py    # 产出 → 交易记录.csv
│
├── 5_买入规则/              ★ 每个买入条件一个独立文件
│   ├── RSI上穿20.py          # def 检查(当前RSI, 前一根RSI)
│   ├── RSI上穿30.py          # def 检查(...)
│   ├── RSI上穿均线.py        # def 检查(...)
│   └── RSI上穿70.py          # def 检查(...)
│
├── 6_卖出规则/              ★ 每个卖出条件一个独立文件
│   ├── 分批止盈.py           # def 检查(盈利比例, 持仓信息)
│   ├── 硬止损.py             # def 检查(亏损比例, ATR)
│   ├── RSI站岗价止损.py      # def 检查(当前RSI, 站岗价, 最低价)
│   ├── 动能衰竭退出.py       # def 检查(RSI峰值, 当前RSI, ATR)
│   ├── 时间退出.py           # def 检查(持仓K线数, 盈亏)
│   └── 背离修正卖出.py       # def 检查(有无底背离, 当前卖出触发)
│
├── 7_仓位管理/
│   ├── 单只仓位计算.py       # def 计算(信号质量, 大盘系数, 波动率)
│   └── 总仓位控制.py         # def 控制(当前持仓数, 总资金, 大盘状态)
│
├── 8_ML训练工厂/            ★ 后续批次才用
│   ├── 训练配置.yaml
│   ├── 一键训练.py
│   ├── 任务1_买入假突破判断/
│   ├── 任务2_盘整判断/
│   ├── 任务3_入场时机判断/
│   ├── 任务4_假跌判断/
│   ├── 任务5_动能衰竭判断/
│   ├── 任务6_背离真伪判断/
│   └── 已训练模型/
│
├── 9_输出/                  ★ 跑完后你直接打开
│   └── K线回放_股票代码_日期.html
│
├── 10_实验记录/             ★ 每次跑自动存档
│   └── 2026-07-17_实验1_说明/
│       ├── 本次配置备份.yaml
│       ├── K线回放.html
│       └── 交易明细.csv
│
└── 启动说明.md
```

### 代码示例（你看得懂的写法）

```python
# 5_买入规则/RSI上穿20.py
# 这个文件只做一件事：检查RSI是否从20以下穿到20以上

def 检查(当前RSI, 上一根RSI):
    """
    输入: 当前这根K线的RSI值, 上一根K线的RSI值
    输出: True=触发了买入信号, False=没触发
    """
    
    if 上一根RSI <= 20 and 当前RSI > 20:
        # 从RSI<=20上穿到>20 → 极端超卖反弹信号
        买入信号 = {
            "触发": True,
            "类型": "RSI上穿20",
            "优先级": 0,          # 最高优先级
            "基础质量分": 1.0      # 最好的信号
        }
        return 买入信号
    
    return {"触发": False}
```

```python
# 6_卖出规则/RSI站岗价止损.py
# 这个文件只做一件事：检查站岗价是否被跌破

def 检查(当前RSI_前复权, 站岗价_前复权, 当前最低价_前复权, ATR值):
    """
    站岗价逻辑:
    RSI下穿80/70/均线(>70) → 设站岗价 = 当前最低价
    后续K线最低价 ≤ 站岗价 - ATR缓冲 → 卖出
    """
    
    if 站岗价_前复权 is None:
        # 还没有站岗价 → 检查是否需要设一个
        return {"触发": False, "需要设站岗价": False, "原因": ""}
    
    缓冲价 = 站岗价_前复权 - 0.5 * ATR值
    
    if 当前最低价_前复权 <= 缓冲价:
        # 跌破缓冲价 → 确认真跌，卖出
        return {
            "触发": True,
            "原因": f"跌破站岗价: 最低价{当前最低价_前复权} < 站岗价{站岗价_前复权}-ATR缓冲{0.5*ATR值}"
        }
    
    return {"触发": False}
```

### 你以后怎么知道改了哪里

```
每次修改后，我告诉你三件事:

  ① 改了什么文件: "修改了 5_买入规则/RSI上穿20.py"
  ② 改了哪里:     "把触发阈值从20改成了25"
  ③ 为什么改:     "因为回测发现RSI=15~20的信号胜率太低"

你打开 Obsidian 看项目跟踪的调试日志
每一行记录一次修改
```

### 项目跟踪看板和代码同步更新

```
每次操作后，项目跟踪看板同步更新:

调试日志:
| 2026-07-17 | 600519 | 创建第一批全部文件 | ✅ 完成 | 
| 2026-07-17 | 600519 | 跑通回测 | ✅ K线回放已生成 |
| 2026-07-17 | — | 修改RSI上穿阈值 20→25 | ✅ 见5_买入规则/RSI上穿20.py |
```

**你打开 Obsidian → 看项目跟踪看板 → 就这张表，就知道全部进度。**
