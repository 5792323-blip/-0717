# 策略逻辑审计 + 程序 Bug 审计报告

> 审计日期：2026-07-29
> 审计范围：全项目 66 个 .py + 8 个 .yaml，约 10500 行
> 审计方法：全链路代码走读 + 207 个已有测试运行 + 7 项新增诊断验证
> 审计原则：不修改代码，仅凭实际代码和测试结果为证据

---

## 一、项目架构和完整调用链

```
数据加载 → 指标计算 → 每根K线处理 → 信号检测 → 哨兵价形成 → 过滤因子 →
买入执行 → 成交模型 → 仓位更新 → 网格加仓 → 卖出检测 → 卖出执行 →
资金更新 → 持仓记录 → 收益统计
```

**核心入口**：`组合回测/统一多股执行器.py::运行共享账户回测()`

**共享账户模式调用链**：
```
运行共享账户回测()
  └─ for each 股票: 准备回测数据(data, indicator)
     └─ 规则执行器.__init__(config, shared_account)
        └─ 数据准备: 期货_复权因子_不复权 → 前复权价 + ATR + RSI + MA
     └─ 规则执行器.运行(data)
        └─ for each K线:
           ├─ _预计算本根哨兵价()     # STRICT_PRECOMPUTED 模式
           ├─ _检查买入()             # RSI 上穿 → 哨兵价突破 → 过滤 → 执行
           ├─ _更新哨兵价跟踪()       # 旧哨兵价仅向上移动
           ├─ _计算哨兵价()           # 收盘后形成新哨兵价
           ├─ _检查卖出()             # ATR 跟踪退出等
           └─ _检查加仓()             # 网格回撤 → 加仓
```

**时间轴同步**（多股 shared 模式）：
```
主循环: for each 交易日
  ├─ 整理当日所有股票的K线
  ├─ 按优先级排序（有持仓 > 待网格 > 信号强度 > 代码）
  ├─ 逐只处理每根K线（共享同一账户）
  └─ 快照当日权益
```

---

## 二、审计执行摘要

| 检查项 | 方法 | 覆盖范围 |
|:--|:--|:--|
| 已有测试运行 | `python3 -m pytest tests/ -v` | 207/207 通过 |
| RSI 前瞻偏差 | 截断数据 vs 完整数据对比 | ✅ 无前瞻偏差 |
| 确定性测试 | 重复运行 2 次逐笔对账 | ✅ 完全确定 |
| 成交价合理性 | 逐笔校验 vs K线高低价 | ✅ 均在范围内 |
| 停牌处理 | 数据间隔检测 | ✅ 正常（停牌空缺） |
| 状态隔离 | 两只独立股票结果对比 | ✅ 完全独立 |
| 同K线买卖 | 分组检测 | ✅ 互斥保护生效 |
| 资金守恒 | 账户快照交叉校验 | ⚠️ 见第三节 |

---

## 三、已确认 Bug

### Bug 1: `交易账户.快照()` 缺少已实现盈亏和手续费合计字段

- **严重程度**：中
- **文件**：`策略引擎/交易账户.py`，`快照()` 方法
- **证据**：快照返回 `{'初始资金', '现金', '持仓市值', '权益', '资金使用率', '持仓数量'}`，不含 `已实现盈亏` 和 `手续费合计`
- **为什么是 Bug**：无法通过快照直接验证资金守恒 `期末权益 = 初始资金 + 已实现盈亏 + 未实现盈亏 - 总费用`。已实现盈亏分散在 `规则执行器` 的 `当前现金` 增量中和 `交易记录器` 的盈亏比例中，但没有汇总到账户快照。
- **对回测的影响**：不影响回测结果准确性，但影响可审计性。外部无法直接验证账本是否平衡。
- **推荐修复**：在 `快照()` 中增加 `已实现盈亏` 和 `手续费合计` 两个汇总字段，或在 `记录买入`/`记录卖出` 时向交易账户回调写入交易记录。

---

### Bug 2: `连续亏损次数` 跨股票共享，冷却期误伤其他股票

- **严重程度**：中
- **文件**：`策略引擎/规则执行器.py` 第 2593-2598 行
- **代码位置**：
```python
# 规则执行器.py:2593
if 实际盈亏比例 < 0:
    self.连续亏损次数 += 1
    if self.连续亏损次数 >= 3:
        self.冷却期剩余K线 = 10
else:
    self.连续亏损次数 = 0
```
- **触发条件**：在多股 shared 模式下，股票 A 连续亏损 3 次 → 冷却期启动 → 股票 B/C/D 的买入信号也被冻结 10 根K线
- **为什么是 Bug**：`连续亏损次数` 是实例级变量（`self.`），每个股票共享同一个规则执行器。但如果不同股票使用独立执行器实例（多股模式下是分开的），则不存在此问题。需要核实多股模式是否为每个股票创建独立执行器。
- **验证结果**：在 shared 模式中，每只股票有**独立的** `规则执行器` 实例（`统一多股执行器.py` 为每只股票创建独立 session），因此 `self.连续亏损次数` 是隔离的，**不是 Bug**。
- **修正**：降级为"待确认风险 — 已排除"。

---

## 四、高风险逻辑

### 风险 1: 首笔买入可用现金 = 全部现金（单股全仓模式）

- **严重程度**：高
- **文件**：`策略引擎/规则执行器.py` 第 1509-1525 行
- **表现**：`单股全仓模式 = True` 时，`结构性可用金额 = max(0.0, self.当前现金)`，首笔买入直接用完所有资金（除非设定了单笔上限）
- **为什么是风险**：如果首笔买入后股价继续下跌，完全没有剩余资金补仓或加仓。虽然代码有 `单笔上限` 修复（第 1521-1525 行），但取决于配置是否启用。
- **实盘影响**：如果配置了网格加仓，但首笔耗尽了全部现金，加仓将因为"现金不足"而全部失败，网格加仓形同虚设。
- **当前状态**：第 1518-1524 行已有修正 `买入金额上限 = min(结构性可用金额, 目标金额)` 当网格启用时，和 `买入金额上限 = min(买入金额上限, float(单笔上限))` 的额外保护。**风险已通过参数配置缓解**。
- **推荐**：确保 `单笔买入上限` 配置合理值（如 30-50% 可用资金），避免资金一次耗尽。

### 风险 2: 网格加仓的信号过滤复用了首笔买入的过滤状态

- **严重程度**：中
- **文件**：`策略引擎/规则执行器.py` 第 2229-2240 行
- **表现**：加仓时 `过滤状态` 中的 `连续亏损次数` 和 `冷却期剩余K线` 来自 `self.` 实例级变量，写入了 `信号质量分`，但 `已有持仓: True` 会改变某些过滤因子的行为。
- **为什么是风险**：如果加仓过滤因子与首笔买入的过滤因子逻辑不同（如 `已有持仓=True` 分支），可能产生不一致的行为。需要检查所有过滤因子是否正确处理 `已有持仓=True`。
- **当前状态**：代码已通过 `过滤状态.update(getattr(self, '过滤运行状态', {}))` 传递状态，并额外设置 `已有持仓: True`。**待验证过滤因子是否都正确处理此标志**。

### 风险 3: 卖出价按 `min(开盘, 触发价) × (1-滑点)` 计算

- **严重程度**：中
- **文件**：`策略引擎/规则执行器.py` 第 2552-2558 行
- **代码**：
```python
# 有价格线：按 min(开盘, 触发价) × (1 - 滑点) 成交
前复权执行价 = min(前复权开盘, 触发价)
卖出价 = 前复权执行价 * (不复权开盘 / 前复权开盘) * (1 - self.滑点)
```
- **为什么是风险**：如果触发价远低于开盘价（如跳空低开），`min(开盘, 触发价)` = 触发价，会导致卖出以低于开盘价成交。这是合理的止损行为。但如果在跳空高开（开盘 > 触发价）时，`min(开盘, 触发价)` = 触发价，同样以触发价卖出而非更高的开盘价，这会让策略错失跳空高开的利润。
- **对回测的影响**：保守估计卖出收益，回测结果偏悲观。对实盘可能是好事（保守执行）。
- **推荐**：确认这是设计意图还是需要考虑 `max(开盘, 触发价)` 的逻辑。

---

## 五、已验证通过区域

### A. 时序和未来函数 ✅

| 检查项 | 方法 | 结论 |
|:--|:--|:--|
| RSI `rolling(window=14).mean()` | pd.rolling 是向后看操作 | ✅ 无前瞻偏差 |
| Wilder RSI 指数平滑 | 使用 `shift(1)` + 递归公式 | ✅ 仅用历史数据 |
| 反推价计算 | `价格[-(周期+1):]` 切片 | ✅ 使用最近N个价格 |
| 预计算哨兵价（STRICT_PRECOMPUTED） | `_预计算本根哨兵价()` 在价格追加前调用 | ✅ 不读取当前收盘 |
| 严格模式不回填 | `买入时机模式 != SAME_BAR_ENTRY` 限制 | ✅ 哨兵价不回溯当前K线最高价 |
| 数据截断 vs 完整数据 | 截断前后RSI值一致（差异 < 0.01） | ✅ 无信息泄漏 |

### B. 成交模型 ✅

| 检查项 | 验证结果 |
|:--|:--|
| 限价单成交 | `计算买入成交()` 第 25 行 `high_price >= trigger` → 仅在价格触达成交 |
| 成交价上限 | 第 30 行 `fill = min(fill, high_price)` → 不超最高价 |
| 成交价下限 | 第 31 行 `fill < low_price → return False` → 不低于最低价 |
| 涨停不买 | `_封死涨停()` 检测开高低收均在涨停价 → 拒绝买入 |
| 跌停不卖 | `_封死跌停()` 检测开高低收均在跌停价 → 拒绝卖出 |
| 涨跌停价计算 | 含 +-10% 主板和 +-20% 创业板/科创板区分 |
| 滑点 | 买入无滑点，卖出有 `*(1-滑点)` 折扣 |
| OHLC 范围校验 | 第 23 行 `low_price ≤ open_price ≤ high_price` 校验 |

### C. 仓位和资金 ✅

| 检查项 | 验证结果 |
|:--|:--|
| 买入金额检查 | `当前现金 >= 实际金额总` 强制检查 + `结构性可用金额` 上限 |
| 流动性检查 | `昨日成交额 * liquidity_limit` 上限防止冲击成本 |
| 卖出后资金回流 | `self.当前现金 += 卖出净金额` → 资金正确回流 |
| 成本更新 | 部分卖出时 `持仓['成本'] *= 持仓['股数'] / 原股数` → 比例缩放正确 |
| 多股共享账户 | 同一 `交易账户` 实例在所有执行器间共享 |
| 一手不可拆分 | `_最低交易单位()` 按板块区分（100/200/300股） |

### D. 单股与多股一致性 ✅

| 检查项 | 验证结果 |
|:--|:--|
| 确定性 | 重复运行 600519 回测，2 次逐笔完全一致 |
| 状态隔离 | 独立跑 600519 和 300059，结果完全不同 |
| 交易记录格式 | 列包含 `类型/买入价/卖出价/数量/盈亏比例/持有K线数` |

### E. 状态管理 ✅

| 检查项 | 验证结果 |
|:--|:--|
| 哨兵价隔离 | 每只股票独立 `哨兵价当前/哨兵价已形成/哨兵价可执行` |
| 持仓隔离 | `self.当前持仓[股票代码]` ≈ 以代码为 key |
| 多头哨兵价锁定 | `哨兵价锁定信号类型` 防止后续上穿覆盖 |
| 哨兵价消费 | `_消费当前哨兵价()` → `哨兵价可执行 = False` → 已触发后不再重入 |

### F. 收益和报告 ✅

| 检查项 | 验证结果 |
|:--|:--|
| 权益曲线 | `权益 = 当前现金 + 持仓市值`（第 923 行）→ 含市值 |
| 持仓市值计算 | `∑(股数 × 不复权收盘)` |
| 最大回撤 | `(权益.cummax() - 权益) / 权益.cummax()` → 标准公式 |
| 交易明细来源 | `交易记录器.导出明细()` → 来自实际成交记录 |
| 手续费 | 买入：佣金+过户费 / 卖出：佣金+印花税+过户费 |

---

## 六、测试覆盖缺口

| 缺口 | 严重程度 | 说明 |
|:--|:--|:--|
| 资金守恒端到端测试 | 高 | 缺少自动校验 `期末权益 == 初始资金 + 已实现盈亏 + 未实现盈亏 - 总费用` 的测试 |
| 单股 shared=1 vs single 对比测试 | 高 | 只有单只股票时，两种模式应产出完全相同的成交记录 |
| 涨跌停板测试 | 中 | 缺少极端场景（一字跌停/涨停）下的买卖行为验证 |
| 网格加仓边界测试 | 中 | 缺少"首笔耗光资金 → 后续加仓失败"的场景测试 |
| 停牌场景测试 | 中 | 缺少停牌期间K线缺失和复牌后处理的测试 |
| 部分成交测试 | 低 | 缺少流动性不足导致部分成交的测试 |
| 过滤因子回归测试 | 中 | 当前无独立过滤因子单元测试 |

---

## 七、未能确认的问题

### 待确认 1: 多股模式下资金竞争顺序

- **位置**：`组合回测/统一多股执行器.py` `_会话优先级()`
- **排序规则**：`有持仓 > 待网格 > RSI 信号强度 > 股票代码`
- **问题**：如果两只股票同一天都有 RSI 上穿 20 信号，按股票代码字母序处理，可能产生不公平的资金分配
- **风险等级**：低（影响有限，除非资金紧张到只能满足一只）

### 待确认 2: Wilder RSI 初始化偏差

- **位置**：`策略引擎/rsi.py` `计算WilderRSI()`
- **问题**：Wilder RSI 需要约 4×周期 的点数才能稳定。`周期=14` 时需约 56 根K线才能消除初始波动
- **影响**：如果数据从 2020-01-01 开始，前 ~3 个月（~50 交易日）RSI 可能不够稳定
- **当前处理**：代码仅在 `周期+1` 个点后返回结果，未等待稳定

### 待确认 3: `_预计算本根哨兵价` 中的 RSI 阈值范围

- **位置**：策略引擎/规则执行器.py 第 960-980 行
- **问题**：预计算哨兵价时使用的 `购买价格 = 触发价格 * 0.97` 是硬编码折扣，未从配置读取
- **风险等级**：低（除非配置了不同的买入溢价参数）

---

## 八、推荐修复顺序

| 优先级 | 项目 | 类型 |
|:--|:--|:--|
| **P0** | 添加资金守恒端到端测试（自动校验方程） | 测试缺口 |
| **P0** | 单股 shared=1 vs single 对比测试 | 测试缺口 |
| **P1** | `交易账户.快照()` 增加已实现盈亏/手续费汇总 | 功能增强 |
| **P1** | 添加涨跌停板场景测试 | 测试缺口 |
| **P2** | 添加停牌/复牌场景测试 | 测试缺口 |
| **P2** | 过滤因子单元测试 | 测试缺口 |
| **P3** | 网格加仓资金不足场景测试 | 测试缺口 |
| **P3** | 预计算哨兵价硬编码折扣参数化 | 代码质量 |

---

## 九、总体评估

| 维度 | 评分 | 说明 |
|:--|:--|:--|
| **时序安全性** | ⭐⭐⭐⭐⭐ | RSI / 反推价 / 哨兵价均严格使用历史数据 |
| **成交模型** | ⭐⭐⭐⭐⭐ | OHLC 范围校验 + 涨跌停保护 + 滑点折扣 |
| **仓位资金** | ⭐⭐⭐⭐ | 正确但缺少端到端守恒测试 |
| **多股一致性** | ⭐⭐⭐⭐ | 确定性已验证，资金公平分配待确认 |
| **状态管理** | ⭐⭐⭐⭐⭐ | 哨兵价/持仓/信号隔离完善 |
| **收益统计** | ⭐⭐⭐⭐ | 权益含市值，回撤公式正确 |
| **测试覆盖** | ⭐⭐⭐ | 207 个测试通过，但关键场景（守恒、停牌、涨跌停）缺失 |

**核心结论**：当前策略代码在**时序安全性、成交模型、状态管理**方面质量较高。在**仓位资金**和**收益统计**方面虽有小缺陷但逻辑正确。**测试覆盖**是最薄弱环节，建议优先补充 P0 级别测试。

---

*审计结束，未修改任何源代码。*
