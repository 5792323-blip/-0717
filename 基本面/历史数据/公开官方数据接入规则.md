# 历史行业评分数据接入规则

默认先使用公开官方数据。每一行必须填写实际可得的`生效日期`和不晚于该日期的`数据截止日`；报告期末不能替代发布日期。

同一`指标键 + 生效日期`允许后续替换：构建器保留`来源优先级`更高的记录；优先级相同则保留数据截止日较晚的记录。

建议优先级：公开官方数据 `10`；经人工核验的公开原始文件 `50`；用户确认的Wind/Choice/iFinD/Tushare导出 `100`。替换只影响对应指标和时点，原始导入文件应保留以便审计。
