# 公开数据更新

运行检查（不改工作簿）：

```bash
python3 基本面/公开数据更新.py
```

写入通过时效校验的数据：

```bash
python3 基本面/公开数据更新.py --apply
```

更新器只写入最新值、发布日期和来源备注，不自动修改“当前评分(1-10)”。评分须按 v3 工作簿中的行业标准与评分口径复核。数据早于工作簿基准日或超过最大时效时，会被跳过并记录在 `运行记录/最新公开数据更新报告.json`。

免费公开源仅适合宏观与部分高频序列。订单、招标、临床、渠道、项目回款等深度行业数据保持待核验，不能伪造为实时数据。
