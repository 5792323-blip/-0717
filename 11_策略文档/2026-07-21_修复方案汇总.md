# 2026-07-21 - ��略0717修复方案汇总

## 讨论结论

系统需要同时解决三类问题:

1. 回测口径和执行链路不够稳定，导致因子比较不可靠。
2. 回放页面需要接近看盘软件，支持全历史、联动幅图和可解释交易。
3. 多股票需要一个统一工作台，用户通过页面选择股票，不阅读代码。

## 实施顺序

`冻结回测口径 -> 修复信号追踪 -> 单股票看盘回放 -> 多股票工作台 -> 单个买入信号 -> 单个过滤因子 -> 单个退出规则 -> 市场状态 -> 仓位管理 -> ML`

## 当前状态

- 多股票回放入口已生成。
- 右侧信息区已改为固定分区比例。
- K 线全历史导航和 RSI 联动仍属于下一阶段页面修复内容。
- 尚未开始新的策略因子调参。

## 第 1 步进展

- 已修正最终权益不含未平仓持仓的问题。
- 已让滑点、佣金、印花税、过户费和买入溢价从参数配置读取。
- 已补充最大回撤、平均盈利、平均亏损和盈亏比指标。
- 报告生成器不再根据最终现金和收益反推初始资金，优先使用本次回测真实初始资金。
- 600519（2020-01-02 至 2024-12-31，初始资金 2000 万）验证结果：最终权益 19,612,596 元，总收益率 -1.94%，最大回撤 2.14%，326 买/326 卖，胜率 32.5%，盈亏比 0.50。

## RSI 信号类型修复

- 哨兵价形成时保存 `哨兵价锁定信号类型`；新的 RSI 上穿信号出现时覆盖尚未成交的旧哨兵价。
- 买入成功后释放锁定，允许下一笔信号正常形成。
- 旧哨兵价不再依赖 72 根 K 线自动过期，新上穿信号直接替换旧信号。

## RSI新上穿覆盖旧哨兵价

- 已修改哨兵价生成逻辑：新的 RSI 上穿 20/30/RSI_MA/70 出现时，直接重算并覆盖尚未成交的旧哨兵价。
- 覆盖后，信号类型、哨兵价、形成索引和等待计数全部切换到新信号。
- 这样不会因为旧的 RSI上穿70 哨兵价长期等待，而阻塞后续 RSI上穿20 信号。
- 买入失败时不释放锁定，避免记录出一个并未成交的虚假新信号。
- 600519（2020-01-02 至 2024-12-31）修复后信号统计：上穿20 24笔、上穿30 26笔、上穿均线 63笔、上穿70 57笔；总计170笔买卖配对。
- 修复后结果：最终权益 19,842,988 元，总收益率 -0.79%，最大回撤 0.97%，胜率 35.3%，平均盈亏 -0.49%。该差异来自信号等待/过期逻辑恢复真实行为，不能单独视为策略改善。

## 过滤因子执行链路修复

- 根因：`8_过滤因子` 只被加载，买入流程构造过滤状态后直接绕过执行；实际调用的是另一套 `因子模块`。
- 修复：`过滤因子配置.yaml` 中启用的过滤因子现在逐个执行，任一因子不通过就拦截买入。
- 异常安全：因子执行异常按拦截处理，并在 K 线决策中记录异常原因。
- 回放记录：每根 K 线新增过滤因子明细，显示每个因子通过/拦截及原因。
- 当前配置中的过滤因子仍全部关闭，因此本次不改变基线交易结果；后续启用单个因子时可以做真实 A/B 回测。

## 每笔交易决策记录完善

- 每根 K 线新增统一 `决策记录`，包含决策阶段、买入链路、过滤结果、卖出链路和最终动作。
- 买入链路记录信号类型、哨兵价、形成索引、等待 K 线数、突破状态、成交价、数量、金额、费用和质量分。
- 卖出链路记录持有 K 线数、当前盈亏、RSI 峰值、当前 RSI、最高价、每条卖出规则结果、触发价、成交价、费用和实际净收入。
- 交易明细中的买入/卖出记录保存决策快照，卖出完成后回填到对应买入记录。
- 回放页面当前 K 线决策面板显示决策阶段、买入链路和卖出链路摘要。

## 看盘式回放第一轮完成

- 默认回放窗口调整为最多 120 根 K 线，保持 K 线可读。
- 增加“全量”按钮，可将全部历史 K 线压缩到总览视图。
- 主图和 RSI 幅图使用同一 hover 索引。
- RSI 幅图增加同步竖向十字线、当前 RSI 数值标签和 RSI_MA 数值标签。
- 保留底部导航、拖拽、滚轮平移和缩放；缩放可从全量模式返回可读窗口。
- 600519 基线回放已重新生成，结果仍为总收益率 -0.79%，170 买/170 卖。

## 第一个单因子实验：RSI 位置过滤

- 实验对象：600519，2020-01-02 至 2024-12-31，初始资金 2000 万。
- 唯一变化：启用 RSI 位置过滤，阈值 `RSI > 70 不买`。
- 基线：170 买/170 卖，总收益率 `-0.79%`，最大回撤 `0.97%`，胜率 `35.3%`，盈亏比 `0.576`。
- 加因子：106 买/106 卖，总收益率 `-0.88%`，最大回撤 `0.94%`，胜率 `33.0%`，盈亏比 `0.379`。
- 过滤统计：297 根候选信号 K 线中，122 次通过，175 次拦截。
- 初步结论：在当前单股和参数下，该因子降低交易数量和最大回撤，但收益、胜率和盈亏比变差，暂不纳入正式基线。
- 配置已恢复为关闭，后续因子实验仍以原始基线为对照。

## 第二个单因子实验：RSI_MA 假信号过滤

- 实验对象：600519，2020-01-02 至 2024-12-31，初始资金 2000 万。
- 唯一变化：启用 `RSI_MA假信号过滤`，检查前 `5` 根 RSI_MA；仅对“RSI上穿均线”信号生效，当前 RSI_MA 突破前 5 根最高值时拦截。
- 基线：170 买/170 卖，总收益率 `-0.79%`，最大回撤 `0.97%`，胜率 `35.3%`，盈亏比 `0.576`。
- 加因子：124 买/124 卖，总收益率 `-0.48%`，最大回撤 `0.54%`，胜率 `37.1%`，盈亏比 `0.598`。
- 过滤统计：248 根候选 K 线中，139 次通过、109 次拦截；非均线信号均记录为通过，说明因子按信号类型参与执行。
- 初步结论：本次单股样本中收益、回撤、胜率和盈亏比均优于基线，作为候选因子保留；暂不直接冻结，需继续做多股票、不同市场阶段和参数敏感性验证。
- 正式配置已恢复关闭，固定 `K线回放_600519.html` 已重新生成基线版本。

## RSI_MA 过滤多股票验证

- 样本：10 只股票（000001、002532、300122、600415、600438、600519、600919、601009、601012、601898），统一使用 2020-01-02 至 2024-12-31、初始资金 2000 万。
- 与关闭因子相比，RSI_MA 检查前 5 根在 10/10 只股票上降低交易次数和最大回撤，平均收益率由 `-0.187%` 改善为 `-0.130%`。
- 胜率在 9/10 只股票上提高；单股收益均改善，但样本整体仍为负收益，不能据此直接正式启用。
- 各股票的过滤检查均有通过/拦截记录，说明该因子确实参与执行，而不是只显示配置。
- 结论：RSI_MA 过滤进入参数敏感性验证；正式配置继续关闭。实验文件保存在 `10_实验记录/20260721_RSI_MA多股票验证/`。

## RSI_MA 参数敏感性验证

- 比较参数：检查前 `3 / 5 / 10` 根 RSI_MA，仍使用上述 10 只股票和同一回测区间。
- 平均结果：N=3 收益率 `-0.128%`、最大回撤 `0.143%`、胜率 `26.16%`；N=5 收益率 `-0.130%`、最大回撤 `0.145%`、胜率 `26.13%`；N=10 收益率 `-0.132%`、最大回撤 `0.147%`、胜率 `25.89%`。
- N=3 在 10 只股票中的 9 只取得最高收益，稳定性略优于 N=5 和 N=10。
- 结论：暂选 N=3 作为候选参数进入分阶段验证，但不修改正式配置；参数结果保存在 `10_实验记录/20260721_RSI_MA参数敏感性/`。

## RSI_MA N=3 分阶段验证

- 分为 2020-2021、2022-2023、2024 三个阶段，仍使用 10 只股票，与关闭过滤因子的基线对照。
- 2020-2021：平均收益率由 `-0.071%` 改善至 `-0.046%`，9/10 只股票改善，平均最大回撤由 `0.096%` 降至 `0.065%`。
- 2022-2023：平均收益率由 `-0.076%` 改善至 `-0.055%`，10/10 只股票改善，平均最大回撤由 `0.085%` 降至 `0.059%`。
- 2024：平均收益率由 `-0.052%` 改善至 `-0.035%`，8/10 只股票改善，平均最大回撤由 `0.056%` 降至 `0.040%`。
- 结论：N=3 在三个阶段均降低平均回撤且收益改善较一致，进入候选正式配置评审；仍需保留关闭配置作为可逆基线。

## RSI_MA 候选正式配置复核

- 已将正式配置暂设为 RSI_MA 过滤启用、检查前 `3` 根。
- 600519 复核结果：123 买/123 卖，总收益率 `-0.48%`，最大回撤 `0.55%`，胜率 `35.8%`，盈亏比 `0.592`。
- 252 次过滤检查中，138 次通过、114 次拦截，固定回放页已覆盖更新。
- 当前状态：候选配置已启用，作为下一阶段 MA 方向过滤的基础配置；仍保留基线实验数据以便回退。

## MA方向过滤实验准备

- 基础配置保持 RSI_MA N=3 开启。
- 当前 MA 方向配置中的 `-999/+999` 仅是占位阈值，实际上不会拦截，因此首轮实验改用设计说明中的斜率阈值 `-1.5/+1.5`，对比前 5 根 MA。
- 本轮只新增 MA 方向过滤，不修改买入信号、退出规则和仓位参数；实验结束后恢复 MA 过滤关闭。

## MA方向过滤首轮结果

- 规则：斜率拦截，前 5 根 MA，阈值 `-1.5/+1.5`；基础配置为 RSI_MA N=3。
- 10 只股票平均收益率由 `-0.128%` 改善为 `-0.105%`，平均最大回撤由 `0.143%` 降至 `0.126%`。
- 10 只股票中 8 只收益改善，平均胜率由 `26.16%` 降至 `25.37%`，说明收益和回撤改善并未同步带来胜率提升。
- 每只股票均有 MA 通过/拦截审计记录；首轮结论为候选改善，暂不与 RSI_MA 合并冻结，下一步做 MA 阈值敏感性验证。
- 实验结果保存在 `10_实验记录/20260721_MA方向过滤实验/`，正式配置已恢复 MA 关闭、RSI_MA N=3 开启。

## MA方向斜率阈值敏感性

- 对比阈值：`±0.5`、`±1.5`、`±3.0`，前 5 根 MA，基础配置保持 RSI_MA N=3。
- `±0.5`：平均收益率 `-0.044%`，平均最大回撤 `0.057%`，平均胜率 `26.50%`，平均每股买入 58.5 次。
- `±1.5`：平均收益率 `-0.105%`，平均最大回撤 `0.126%`，平均胜率 `25.37%`，平均每股买入 119.3 次。
- `±3.0`：平均收益率 `-0.140%`，平均最大回撤 `0.154%`，平均胜率 `25.96%`，平均每股买入 144 次。
- 结论：`±0.5` 综合最优，但过滤较强、交易显著减少，进入分阶段验证；MA 正式配置仍关闭。

## MA方向 ±0.5 分阶段验证

- 2020-2021：平均收益率 `-0.046% → -0.009%`，平均最大回撤 `0.065% → 0.031%`，10/10 只股票收益改善。
- 2022-2023：平均收益率 `-0.055% → -0.026%`，平均最大回撤 `0.059% → 0.030%`，10/10 只股票收益改善。
- 2024：平均收益率 `-0.035% → -0.009%`，平均最大回撤 `0.040% → 0.013%`，8/10 只股票收益改善；平均每只股票仅约 11.7 笔交易。
- 结论：MA ±0.5 在三个阶段均显著降低回撤且收益改善，进入候选组合配置评审；需特别关注交易次数过少和收益是否被过度过滤。

## 哨兵价突破最小基线

- 新增开关 `哨兵价本根形成立即买入`；本实验关闭该开关，只允许后续 K 线真实突破哨兵价买入。
- 同时关闭全部 `8_过滤因子`，保留交易成本、卖出规则、仓位和风险控制。
- 10 只股票平均结果：173.6 买入/只，收益率 `-0.141%`，最大回撤 `0.168%`，胜率 `25.64%`。
- 600519：157 买/157 卖，收益率 `-0.60%`，最大回撤 `0.77%`，胜率 `35.7%`，盈亏比 `0.629`。
- 结论：哨兵价后续突破单独运行仍未产生正收益，但可作为逐层添加条件的最小基线；正式配置已恢复原状态。

## 哨兵价回放显示修正

- 发现回放中“哨兵价已形成”容易被误读为“本根应该买入”：哨兵价线会在形成后持续显示，只有本根形成即时买入或后续 K 线突破且通过过滤、资金和持仓检查时才成交。
- 回放买入检查新增“哨兵价状态”：`本根形成`、`后续已突破`、`等待突破`，并继续显示未成交的具体原因。
- 当前 600519 正式回放中，未成交原因可区分为 RSI_MA 拦截、可买数量不足、已有持仓等，不再只显示模糊的“没有满足的策略条件”。

## 哨兵价逻辑回测复核

- 临时配置：关闭“本根形成立即买入”，关闭全部 8_过滤因子，只允许后续 K 线突破哨兵价买入。
- 600519 结果：152 买/152 卖，收益率 `-0.83%`，最大回撤 `0.96%`，胜率 `30.3%`，盈亏比 `0.484`。
- 逻辑核对：80 根 K 线为“本根形成并等待后续突破”，168 次为后续突破检查；本根形成直接买入违规记录 `0`。
- 另有 16 次价格已突破但未成交，原因已记录为买入执行未成交，主要用于继续检查资金、持仓和一手数量限制。
- 结论：本次回测符合“本根不买、后续突破才买”的预期；正式配置已恢复，固定回放页已重新生成正式版本。

## 记录要求

## 旧哨兵价自动过期逻辑屏蔽

- 已屏蔽待成交哨兵价超过 72 根 K 线自动释放的逻辑。
- 旧哨兵价现在不会因等待时间自动清除，会持续等待价格突破或实际买入成交。
- 参数文件保留 `信号过期K线数: 72` 作为历史记录，但当前不参与执行。

## 同一根K线买卖修复

- 根因：回测原流程先买入，再用当前持仓检查卖出，导致新买入持仓在同一根 K 线被 ATR 等规则立即卖出。
- 修复：卖出检查跳过本根刚买入的持仓，新买入从下一根 K 线开始接受卖出规则。
- 600519 复核：2020-05-29 10:30 买入 100 股后持有至后续卖出，不再同根卖出；同一时间戳同时出现买入和卖出的记录为 `0`。

## 买入信号配置接入

- `基础质量分` 已按信号类型读取并写入每根 K 线决策记录。
- `单笔买入上限` 已参与买入金额计算，和基础单只金额取较小值。
- `需要均线上涨确认` 与 `确认K线数` 已接入 RSI上穿均线信号；确认根数为 `0` 时跳过确认。
- 验证：当前配置 238 买/238 卖；临时关闭 RSI上穿30 后为 211 买/211 卖，证明启用开关已实际影响回测。

每完成一个步骤，都必须更新本方案、项目跟踪和对应决策日志，写明改动前后、实验范围、指标结果和是否保留。

## 买入点最终口径复核

- 预挂阶段只计算哨兵价，不执行买入；必须等 RSI 实际上穿目标阈值后，才允许当前K线最高价触发买入。
- 2020-02-03 15:00 的反推价为 `1190.80`，该根不买；2020-02-04 10:30 RSI 从 `18.77` 上穿到 `39.49` 且最高价突破，记录买入，哨兵价为 `1190.80`。
- 买入箭头和交易列表统一显示实际成交价 `前复权成交价`，而不是哨兵价或开盘基准价。
- 最终回测：600519，256 买/256 卖，收益率 `+0.51%`，最大回撤 `0.25%`，测试 `7 passed`。

## 买入点再次修正：反推价提前布置

- 根因：旧逻辑只有在 RSI 已经上穿阈值的收盘K线结束后才形成哨兵价，导致用户案例中 2020-02-04 10:30 的上穿与当前K线突破无法按预期成交。
- 修复：当上一根收盘 RSI 仍低于目标阈值时，使用上一根之前的历史价格窗口计算反推价并提前布置哨兵价；下一根K线价格突破后成交，不读取当前收盘确认买入。
- 回放买入箭头统一使用 `前复权成交价`；该价格不再使用仅供计算的“前复权买入基准价”。
- 600519 全历史复核：297 买/297 卖，总收益率 `-0.08%`，最大回撤 `0.67%`，剩余持仓 `0`；测试 `7 passed`。
- 2020-02 案例现按“前一根收盘提前布置、后续K线突破成交”口径重新生成固定回放页；正式页面为 `9_输出/K线回放_600519.html`。
