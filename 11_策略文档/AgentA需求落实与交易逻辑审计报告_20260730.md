# Agent A：需求落实与交易逻辑审计报告

审计角色：需求落实与交易逻辑审计员  
审计模式：只读审计，未修改代码  
项目：`/Users/chen/Desktop/策略0717`  
审计运行编号：`agentA_多股_运行`

## 一、总体结论

当前实现已经具备较完整的运行目录、交易生命周期字段、审批记录和可信度审计能力。以当前工作区配置运行 600519、000001（2020-01-02 至 2021-12-31）的样本中：254 笔成交、254 个唯一 `execution_id`、254 条审批成交记录、43 条组合层拦截，可信度审计为 `PASS`。

但仍有关键缺口：

- CLI 运行清单没有 `experiment_id`，不满足“代码、配置、数据、股票池、区间、费用和账户模式共同决定实验身份”的完整要求。
- CLI 运行清单没有 `费用配置哈希`、`数据版本`和完整股票池版本字段；页面入口的 `experiment_id` 也没有明确包含费用和独立数据哈希。
- 交易生命周期字段在成交文件中有效，但审批记录没有 `intent_id`、`order_id`，因此“交易意图→审批→订单→成交”的完整前向关联尚未证明。
- 多股回放台主要展示汇总和个股 iframe；页面未提供一笔拒绝/部分成交的完整生命周期详情面板，审批、订单、账户变化不能从页面逐层点击验证。
- 当前工作区配置已改变原始策略时序：第一笔交易由基线 2020-02-04 RSI上穿20 变为 2020-01-13 RSI上穿70；原始历史基线不再适用。
- 当前测试虽然为 `246 passed`，但包含其他 agent 的未提交修改，不能作为稳定最终基线。

最终准入结论：**暂不建议进入下一阶段**。必须先确认并冻结并行配置，补齐实验身份和审批生命周期关联，再重新生成基线和逐笔对账。

## 二、审计运行证据

运行命令：

```text
python3 运行程序/run_backtest.py --config 1_策略配置 \
  --stocks 600519,000001 --start 2020-01-02 --end 2021-12-31 \
  --capital 2000000 --workers 1 --portfolio-mode shared \
  --output /tmp/agentA_多股.json
```

生成目录：`/tmp/agentA_多股_运行`

- 文件：`运行清单.json`、`回测结果.json`、`交易明细.csv`、`审批记录.csv`、`可信度审计.json`。
- 成交：254 笔（买入 127、卖出 127）。
- 股票：600519 104 笔，000001 150 笔。
- 审批：实际成交 254 行，组合层拦截 43 行。
- 可信度：`PASS`；资金、持仓、费用、价格、未来数据和审批对账均为 `PASS`。
- 部分成交：本次样本为 0 行，状态应为 `NOT_RUN`，不能写成通过。

## 三、需求—代码—数据—页面映射矩阵

等级：已完整落实、已部分落实、只有页面展示，核心未落实、只有字段定义，没有运行数据、尚未落实、实现错误、无法验证。

| 需求 | 实现位置 | 数据字段/文件 | 页面读取 | 测试/运行证据 | 结论 |
|---|---|---|---|---|---|
| `run_id` 每次唯一 | `运行程序/interactive_backtest_app.py::创建运行目录` | 时间戳目录、`运行清单.json` | 页面使用运行目录和 token | 运行目录 `agentA_多股_运行` | 已部分落实：时间戳足以区分通常运行，但没有并发碰撞测试 |
| `experiment_id` 由完整实验事实决定 | `interactive_backtest_app.py::写入运行清单` | 页面入口有 `experiment_id` | 页面未直接展示该字段 | 代码检查 | 已部分落实：页面入口计算了配置/股票/区间/账户模式哈希，但未明确纳入费用和完整数据内容 |
| CLI 与页面身份一致 | `run_backtest.py::main` | CLI `运行清单.json` | 页面运行清单 | `/tmp/agentA_多股_运行` | 实现错误：CLI 清单没有 `experiment_id` |
| 页面编号来自运行清单 | `interactive_backtest_app.py::首页`、`恢复最近多股报告` | `运行清单.json`、目录名 | 首页和回放 URL | 路由测试通过 | 已部分落实：结果目录来自清单流程，但页面显示的输出名直接使用目录 basename |
| 交易生命周期编号 | `策略引擎/交易记录器.py`、`run_backtest.py` | `intent_id`、`order_id`、`execution_id` | 个股回放读取交易明细 | 254 笔成交均有有效值 | 已部分落实 |
| 拒绝编号和原因 | `统一多股执行器.py`、`运行程序/可信度审计.py` | `rejection_id`、`拒绝分类`、`详细原因`、`审批记录.csv` | 主页面拒绝汇总和明细接口 | 43 条拒绝均有编号和原因 | 已部分落实：页面可汇总，但没有逐笔生命周期面板 |
| 交易意图→审批→订单→成交 | 交易记录器、组合审批、CLI 导出 | 成交有三类编号；审批只有成交/拒绝字段 | 回放台未完整展示审批关联 | 254 条审批与成交数量一致 | 已部分落实：审批记录缺 `intent_id`、`order_id`，完整关联尚未证明 |
| 唯一真实账户来源 | `规则执行器.py`、`交易账户.py`、`统一多股执行器.py` | 交易明细、审批前后现金/持仓 | 页面消费结果文件 | 逐笔账务审计 PASS | 已完整落实（当前样本） |
| 页面不重算成交事实 | `report_generator.py::生成报告` | 持仓过程和交易明细 | 单股/个股回放 | `test_report_uses_real_holding_sentinel_without_recomputing` | 已完整落实（已覆盖哨兵重算风险） |
| 资金/持仓/费用守恒 | `运行程序/可信度审计.py::审计运行目录` | `可信度审计.json` | 首页显示可信度状态 | 当前样本全部 PASS | 已完整落实（当前样本） |
| 缺失数据状态区分 | `可信度审计.py` | `WARNING`、`FAIL`、`PASS` | 首页显示状态 | 无交易单股样本曾为 WARNING | 已部分落实：`NOT_RUN` 和 `NOT_APPLICABLE` 未统一输出 |
| 页面核心指标来自结果文件 | `interactive_backtest_app.py::首页` | `多股回测结果.json`、回测进度 | 首页指标 | 路由测试通过 | 已部分落实：部分页面实时指标来自内存 `live`，尚未逐字段对账 |
| 胜率口径明确 | `汇总结果`、`运行多股回测` | `加权胜率`、股票级盈亏比例 | 首页/回放 | 样本可读取 | 已部分落实：页面没有显示“按已平仓交易/按股票”的口径说明 |
| 缺失费用与零费用区分 | `可信度审计.py` | `fee_coverage` | 首页只显示累计费用 | 审计检查覆盖率 | 已部分落实：覆盖检查存在，但页面不区分缺失和合法零费用 |
| 拒绝订单按原因分类 | `统一多股执行器.py`、页面 JS | `拒绝分类汇总`、`拒绝原因汇总` | 主页面拒绝汇总 | 43 条拒绝均有分类 | 已完整落实（汇总层面） |
| 多股回放区分信号/审批/成交/拒绝 | `生成多股策略回放页面` | 主要是股票汇总和 iframe | 多股回放台 | HTML 路由通过 | 已部分落实：页面未提供完整事件类型时间线 |
| 保存分层和安全删除 | `结果生命周期.py` | 核心文件集合、锁定标志 | 生命周期接口 | 生命周期测试通过 | 已部分落实：清理前没有生成影响预览；深度文件删除后的页面提示未证明 |
| 历史基线保护 | `结果生命周期.py::锁定` | `locked` | 页面按钮 | 锁定后清理测试通过 | 已完整落实（锁定阻断清理） |

## 四、纵向追踪结果

### 3 笔买入

| 股票/日期 | intent/order/execution | 价格/数量/费用 | 现金持仓字段 | 结论 |
|---|---|---|---|---|
| 600519 / 2020-01-13 | `600519:I00000001` / `600519:O00000001` / `600519:X00000001` | 1117.20 / 100 / 29.0472 | 审批前现金 2,000,000，审批后 1,888,250.9528；持仓 0→100 | 成交和账务可追踪 |
| 600519 / 2020-02-04 | `600519:I00000003` / `600519:O00000003` / `600519:X00000003` | 1016.01 / 100 / 26.41626 | 交易明细总成本 101,627.41626 | 成交和费用可追踪 |
| 600519 / 2020-02-11 | `600519:I00000005` / `600519:O00000005` / `600519:X00000005` | 1077.08 / 100 / 28.00408 | 总成本守恒检查通过 | 成交和费用可追踪 |

### 3 笔卖出

| 股票/日期 | intent/order/execution | 价格/数量/费用 | 结果 |
|---|---|---|---|
| 600519 / 2020-01-14 | `600519:I00000002` / `600519:O00000002` / `600519:X00000002` | 1114.84 / 100 / 140.46984 | 卖出净额 111,343.53016 |
| 600519 / 2020-02-10 | `600519:I00000004` / `600519:O00000004` / `600519:X00000004` | 1059.12 / 100 / 133.44912 | 卖出净额 105,778.55088 |
| 600519 / 2020-02-14 | `600519:I00000006` / `600519:O00000006` / `600519:X00000006` | 1085.69 / 100 / 136.79694 | 卖出净额 108,432.20306 |

### 2 笔拒绝

| 股票/日期 | rejection_id | 原因/分类 | 前后现金/持仓 |
|---|---|---|---|
| 600519 / 2021-01-07 | `R00000001` | 资金不足一手 / 策略或单只仓位上限 | 现金不变；持仓 0→0 |
| 600519 / 2021-01-08 | `R00000002` | 资金不足一手 / 策略或单只仓位上限 | 现金不变；持仓 0→0 |

### 股票和亏损贡献

- 成交最多股票：000001，150 笔。
- 样本内卖出盈亏比例合计较低者：000001，亏损比例合计约 `-1.16114`；600519 约 `-0.34305`。
- 部分审批/部分成交：本次运行 0 条，状态 `NOT_RUN`。

## 五、页面与交易核心一致性

已验证：

- 主页面多股结果读取 `多股回测结果.json`，可信度状态读取 `可信度审计.json`。
- 多股回放台由 `生成多股策略回放页面` 使用 `summary`、`details`、`组合权益曲线` 生成。
- 个股回放从同一运行目录读取 `回放行情.csv.gz`、`交易明细.csv`、`持仓过程.csv`。
- 页面没有通过成交价格重新计算账户现金；成交事实来自结果文件。

未完整验证：

- 页面上的组合最终权益、胜率、拒绝数、累计费用与同一运行目录文件逐字段自动对账。
- 点击某一笔拒绝或成交后，页面是否能显示审批前后现金、持仓、申请金额、批准金额和四类生命周期编号。
- 真实浏览器视觉交互；本轮只有 Flask 路由和页面相关测试证据。

## 六、最严重的 5 个风险

1. 当前并行配置改变了正式交易行为，原基线不再适用；第一笔差异为 2020-01-13 600519 RSI上穿70。
2. CLI 与页面的 `experiment_id` 契约不一致，CLI 运行无法可靠识别相同实验。
3. 审批记录没有意图和订单编号，无法完成完整生命周期前向追踪。
4. 页面主要显示汇总，拒绝/部分成交/审批前后账户变化尚未形成逐笔可点击证据链。
5. 状态模型没有统一 `NOT_RUN`/`NOT_APPLICABLE`，无交易或缺失数据可能被误读为通过或普通警告。

## 七、建议新增测试

- 相同代码、配置、数据、股票池、区间、费用和账户模式生成相同 `experiment_id`；任何一项变化生成不同值。
- CLI 与页面同一实验生成相同实验身份字段。
- 每条成交审批记录必须可通过 `intent_id`、`order_id` 找到对应订单意图；每条拒绝必须有 `rejection_id` 和分类。
- 页面指标与 `多股回测结果.json`、`审批记录.csv`、`可信度审计.json` 自动逐字段对账。
- 人工构造部分成交，验证页面和审计显示“部分成交”而非“实际成交/拒绝”。
- 删除深度数据后访问个股回放，页面必须明确提示无法完整重放，而不是静默生成不完整页面。
- 并发创建运行目录和实验身份碰撞测试。

## 八、是否允许进入下一阶段

**不允许。**

阻塞条件：冻结并行配置；补齐 CLI `experiment_id`；建立审批到意图/订单的关联；完成页面逐字段对账和部分成交页面验收；重新生成稳定基线后，再进入下一阶段。
