# 配置变更影响审计附录

日期：2026-07-30

## 未提交配置变更

文件：`1_策略配置/参数配置.yaml`

```diff
- 买入时机模式: same_bar_entry
- 首次开仓时机模式: same_bar_entry
+ 买入时机模式: precomputed_stop_entry
+ 首次开仓时机模式: precomputed_stop_entry
```

该变更未由本轮审计修改，也未被回退或提交。

## 验证影响

在当前工作区配置下运行：

```text
python3 -m pytest -q --disable-warnings --maxfail=5
```

结果：前 166 项通过，随后 5 项失败：

1. `tests/test_interactive_backtest_app.py::test_precheck_allows_default_single_setup`
2. `tests/test_shared_account_limits.py::test_shared_account_partially_fills_without_repricing_or_negative_cash`
3. `tests/test_shared_account_limits.py::test_two_stock_engines_share_one_position_limit_and_real_cash`
4. `tests/test_single_backtest_baseline.py::test_verified_single_backtest_with_persistent_sentinel_is_stable`
5. `tests/test_single_multi_parity.py::test_one_stock_shared_mode_matches_single_engine_exactly`

失败表现包括：默认表单预检查拒绝、部分成交样本为 0、单股基线买入次数由预期 109 变为 0，以及单股/共享模式交易明细为空。

## 对照证据

使用 Git 基线版本的 `参数配置.yaml` 临时副本（未覆盖工作区文件）运行单股回测：

- 股票：600519
- 日期：2020-01-01 至 2023-12-31
- 交易数：217
- 买入：109
- 卖出：108
- 最终权益：20,033,605.63476

这证明当前失败与买入时机配置差异相关，不能在未确认配置意图前修改代码或测试。

## 当前结论

- 严格预挂单模式本身已有专项单元测试通过。
- 当前用户配置下的完整历史基线：尚未证明。
- 是否将该配置作为新正式基线：需要用户确认后，重新生成基线指纹、逐笔对账和全量测试结果。
