# 对抗性独立代码审计报告（2026-07-30）

## 审计声明

本轮只读审计，未修改代码、配置、测试、行情、工作簿或其他 agent 的未提交文件。工作区原有修改全部保留。

## 基线记录

- 项目：`/Users/chen/Desktop/策略0717`
- 分支：`adaptive-phase1-infrastructure`
- 最新提交：`1a80d2f 记录买入时机配置变更影响`
- 工作区：存在多项已修改文件和未跟踪文件。
- `pytest -q`：失败，命令输出 `zsh: command not found: pytest`。
- `python3 -m pytest -q`：`247 passed in 21.32s`。

## 发现（按严重程度）

### P0：审批成交对账可被保持总量的错配绕过

- 证据：`运行程序/可信度审计.py:102-110` 只比较审批中“实际成交/部分成交”的 `成交股数`总和与交易明细 `成交数量`总和。
- 复现：复制 `/tmp/audit_small_运行` 到临时目录，将两条成功审批的 `成交股数` 互换，保持总和不变；执行 `审计运行目录(临时目录)`，结果仍为 `overall_status=PASS`，`approval_trade_reconciliation=PASS`。
- 基线运行：`/tmp/audit_small_运行`；交易 16 笔、审批 42 条；交易文件包含 `intent_id/order_id/execution_id`，审批文件不包含这三个字段，仅包含 `rejection_id`。
- 影响：无法证明“交易意图 → 审批 → 订单 → 成交”的逐笔对应关系；不同股票、不同价格或不同费用的成交也可能在总量相同下错配。
- 结论：逐笔审批成交关联尚未证明，当前 `PASS` 不能作为完整生命周期审计结论。

### P1：CLI 运行清单缺少 `experiment_id`

- 代码：`运行程序/run_backtest.py:334-341` 创建清单时只写 `run_id`、区间、股票、资金和配置目录，没有 `experiment_id`、配置哈希、数据版本或股票池版本。
- 对比：页面入口 `运行程序/interactive_backtest_app.py:2860-2895` 会生成 `experiment_id`、`config_hash`、`data_version`、`universe_version`。
- 证据：`/tmp/audit_small_运行/运行清单.json` 的字段集合不含 `experiment_id`。
- 影响：CLI 与页面运行身份不可统一，费用/数据/股票池变化无法通过统一实验身份区分；并发碰撞和跨入口复用尚未证明安全。

### P1：页面持久化工作配置可覆盖正式严格配置

- 正式配置：`1_策略配置/参数配置.yaml` 使用 `precomputed_stop_entry`。
- 持久化文件：`10_实验记录/当前回测工作台配置.json` 中 `single_entry_timing`、`multi_entry_timing` 均为 `same_bar_entry`。
- 代码：`运行程序/interactive_backtest_app.py:2281-2298` 读取最近工作台配置；`2483-2485` 的严格默认值只在没有旧工作配置时生效。
- 浏览器复核：访问 `http://127.0.0.1:8766`，DOM 读取到 `single_entry_timing=same_bar_entry`、`multi_entry_timing=same_bar_entry`；同时页面显示“回测主页面”和“多股策略决策回放台”。
- 影响：正式配置与页面可提交配置不一致，页面回测是否采用严格时序尚未证明；旧基线与当前正式基线不能直接比较。

### P1：逐笔账务审计没有按时间/编号排序重建库存

- 代码：`运行程序/可信度审计.py:74-100` 按 CSV 行顺序累加库存，并未按交易时间、`execution_id` 或组合事件序列排序。
- 影响：证据文件被重排、跨股票文件合并或导出顺序变化时，卖出数量检查可能产生误报或漏报；当前样本通过不证明任意文件顺序均安全。

### P1：页面逐笔生命周期和指标自动对账尚未证明

- 页面代码：`运行程序/interactive_backtest_app.py:1600-1615` 主要通过 iframe 加载报告、单票回放和多股回放；`4711-4730` 由行情快照、交易明细和持仓过程生成个股回放。
- 已验证：页面能够读取运行目录中的结果并显示回放入口。
- 尚未验证：点击单笔成交是否展示审批、订单、费用、现金和持仓前后变化；拒绝订单是否能关联意图；页面指标是否逐字段等于 JSON/CSV/审计结果；部分成交是否单独呈现。

### P1：动态历史成分运行没有标准逐笔证据目录

- 代码：`组合回测/统一多股执行器.py:319-322` 按时间点调用历史成分资格判断。
- 运行摘要：`/tmp/h320_dynamic_summary.json`，320 只、972 时间点、审批 28,843 条、实际成交 16,083 笔、错误数 0。
- 限制：摘要没有对应的 `运行清单.json`、`交易明细.csv`、`审批记录.csv`、`可信度审计.json`，因此动态资格下逐笔资金/持仓/费用审计尚未证明。

### P1：部分成交验证状态为 NOT_RUN

- Agent A 运行：`/tmp/agentA2_多股_运行`，成交 254 笔、审批 297 条、组合层拦截 43 条，可信度审计 `PASS`。
- 该运行部分成交为 0 条，不能证明部分成交数量、现金、费用、持仓和页面展示链路。

### P2：深度数据删除后的降级行为只有代码证据

- 代码：`运行程序/结果生命周期.py:47-62` 只删除 `回放行情.csv.gz`、HTML 和 checkpoint，保留核心交易证据。
- 临时最小复现：清理后核心 `交易明细.csv` 和 `运行清单.json` 保留；个股回放入口 `interactive_backtest_app.py:4711-4727` 在缺少行情快照时返回 HTTP 404“该股票没有可用回放”。
- 结论：删除范围可观察，但页面明确降级提示、锁定后禁止删除、删除后主页面与回放台状态一致性尚未完成端到端证明。

## 资金、持仓、费用与未来函数复核

- 小规模命令：`python3 运行程序/run_backtest.py --stocks 600118,600519 --start 2025-01-01 --end 2025-03-31 --capital 200000 --liquidity-limit 0.01 --output /tmp/audit_small.json`。
- 输出文件：运行清单、回测结果、交易明细 16 行、审批记录 42 行、可信度审计。
- 审计结果：`trade_ledger=PASS`、`approval_trade_reconciliation=PASS`、`approval_cash_ledger=PASS`、`approval_position_ledger=PASS`、`cash_conservation=PASS`、`lookahead=PASS`。
- 单股 `600118`（2025-01-01 至 2025-03-31）时序审计：交易 16 笔，`precomputed_stop_entry`，前视风险 `False`，OHLC 边界违规 0。
- 结论：上述样本的资金、持仓、费用、价格和时序守恒通过；不能外推到审批逐笔错配、文件乱序、部分成交、删除深度数据或页面指标一致性。

## 其他边界

- 北交所真实数据：`数据模块/raw/430001_双价格合并.pkl`、`830001_双价格合并.pkl`、`872925_双价格合并.pkl` 均不存在；真实北交所停牌、涨跌停和成交链路尚未证明。
- 数据不足最小复现：`准备回测数据('600118','2026-06-01','2026-06-10','1_策略配置')` 返回 `None`；当前错误粒度仍主要是“无可用数据或数据不足100行”，异常原因契约完整性尚未证明。
- 重复成交：复制临时证据并将第二条 `execution_id` 改为第一条，审计返回 `FAIL`，重复成交检查有效。

## 最终准入结论

**不允许进入下一阶段。**

原因不是测试失败，而是关键证据链存在可复现缺口：审批总量对账可被错配绕过、CLI 缺少统一实验身份、页面工作配置可偏离正式配置、部分成交为 `NOT_RUN`、动态历史成分没有标准逐笔证据目录、页面逐笔展示和指标自动对账尚未证明。当前 `247 passed` 和小规模 `PASS` 只能证明有限样本，不足以证明系统整体真实、正确、完整。
