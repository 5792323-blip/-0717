# 统一 SMA RSI 基线回测与审计报告

## 一、结论

已使用当前代码、当前正式配置和本地 `600519` 行情重新生成单股基线。该基线与旧的 Wilder 口径基线分开保存，不覆盖旧结果。

新的交易数量、现金和权益结果已生成；资金守恒、交易生命周期编号唯一性和策略异常检查通过。长期旧基线断言不再适用，新的 128 买入 / 127 卖出结果尚未锁定为永久基线，因为页面真实 HTTP 回放和逐笔 OHLC 触发证据仍需单独完成。

审计状态：**WARNING（可复现结果已生成，但尚未允许作为最终正式基线）**。

## 二、运行定义

| 项目 | 值 |
|:--|:--|
| 股票 | `600519` 贵州茅台 |
| 数据区间 | `2020-01-01` 至 `2023-12-31` |
| 初始资金 | `20,000,000` |
| 配置目录 | `1_策略配置` |
| 运行参数 | `流动性上限比例=0.01`，`单股全仓模式=True` |
| 买入时机 | `precomputed_stop_entry` |
| RSI口径 | SMA，周期14 |
| 运行目录 | `10_实验记录/基线回测_SMA_RSI_600519_20260731` |

配置快照已保存在运行目录的 `配置快照/`，交易明细和持仓过程分别保存在 `交易明细.csv`、`持仓过程.csv`。

## 三、新基线结果

| 指标 | 结果 |
|:--|--:|
| 数据行数 | 3,880 |
| 买入次数 | 128 |
| 卖出次数 | 127 |
| 实际成交 | 255 |
| 最终现金 | 19,886,534.171779983 |
| 期末持仓市值 | 172,600.000000000 |
| 最终权益 | 20,059,134.171779983 |
| 总收益率 | 0.29567085889993233% |
| 最大回撤 | 0.2296192870635305% |
| 胜率 | 42.51968503937008% |
| 剩余持仓 | 1 |
| 策略异常记录 | 0 |
| 哨兵本根形成记录 | 517 |

注意：程序内部 `总收益率` 字段按百分数数值返回，因此 `0.29567085889993233` 表示 `0.29567%`，不是 `29.567%`。

## 四、逐笔账务审计

### 4.1 资金守恒

运行目录 `交易明细.csv` 的聚合值：

```text
初始资金 - 买入总成本 + 卖出净收入
= 20,000,000 - 22,368,375.26586 + 22,254,909.437639996
= 19,886,534.171779998
```

持仓过程最后一行现金为 `19,886,534.171779983`，误差约 `1.5e-8`，通过资金守恒检查。

交易费用合计为 `33,890.82822`，已计入买入总成本和卖出净收入。

### 4.2 生命周期编号

255 笔成交均有 `intent_id`、`order_id`、`execution_id`，三列各自均为 255 个非空且唯一值；未发现跨交易重复。

`rejection_id` 在本次运行没有拒绝订单，属于无数据，不应解释为拒绝审计通过。

首三笔成交：

```text
2020-01-13 买入 X00000001 / I00000001 / O00000001
2020-01-14 卖出 X00000002 / I00000002 / O00000002
2020-02-04 买入 X00000003 / I00000003 / O00000003
```

### 4.3 策略异常

持仓过程 `策略异常` 字段未发现异常记录。该结果只能证明本次运行没有被记录的策略异常，不能证明所有异常分支都已覆盖。

## 五、哨兵状态审计

- 持仓过程共 3,880 行，其中 `哨兵价本根新形成=True` 的记录 517 条。
- 有 15 行 `哨兵价` 为空，主要出现在指标预热/尚未形成哨兵的阶段；这类空值不能标记为 PASS，只能解释为“无哨兵事实”。
- 新基线交易明细保存了成交时的 `哨兵价`、`触发价`、`前复权成交价` 和信号K线字段，可用于后续逐笔 OHLC 对账。
- 当前报告尚未完成从原始行情逐笔重算每一笔最高价触发边界，因此“每笔成交均由合法哨兵突破触发”仍为**尚未证明**。

## 六、与旧基线的差异

旧测试 `tests/test_single_backtest_baseline.py` 锁定的是：134 买入、133 卖出及旧摘要哈希。统一 SMA 口径后实际为 128 买入、127 卖出，交易摘要哈希为：

```text
交易明细: 7600c2f7ca7e1b50cc1ce58a637f667f01101c4324124698b66bced0ad4ec406
持仓过程: fca30c042617f22a94f1a9ecb5557b2a920c274fbd2b057777816849bf5c8103
```

该差异是算法口径变化后的预期影响，不应通过恢复 Wilder 代码消除。旧测试目前会失败，说明它需要在完成新基线逐笔审计后更新，而不是直接改断言。

## 七、验证命令

```text
python3 -m pytest -q tests/test_sma_rsi_reverse_price.py tests/test_execution_timing.py tests/test_strict_entry_model.py
46 passed
```

基线生成命令使用 `回测引擎.backtest_engine::跑回测`，并调用 `保存结果` 写入运行目录。

## 八、剩余审计项

1. 用原始行情逐笔验证 128 笔买入和 127 笔卖出的 OHLC、哨兵边界和成交日期。
2. 重新生成严格模式回放 HTML，确认蓝线只读取核心持仓过程，不重新推导哨兵价。
3. 验证页面显示指标与本运行目录的结果文件一致。
4. 完成上述项目后，才更新 `tests/test_single_backtest_baseline.py` 的永久断言。

## 九、最终判定

**新的 SMA 基线已生成，账务和编号审计通过；长期基线最终锁定尚未完成。**

在逐笔 OHLC、严格时序和页面回放完成前，不允许把本报告的 128/127 结果宣称为“策略完全正确”。

## 十、回测页面哨兵价显示修复

报告生成器原先在持仓过程缺少某个 K 线索引时，仍可能按报告层逻辑重新反推哨兵价，造成蓝线与交易核心事实不一致。

已修改 `回测引擎/report_generator.py::生成报告`：只要结果包含真实 `持仓过程`，整份报告禁止哨兵价兜底重算；缺失的核心状态保持为空，不再用示例或报告层推导值伪造蓝线。

新增回归测试 `tests/test_report_generator.py::test_report_does_not_recompute_missing_real_state_sentinel`，并验证：

```text
python3 -m pytest -q tests/test_report_generator.py tests/test_sma_rsi_reverse_price.py
9 passed
```

已重新生成页面：

`10_实验记录/基线回测_SMA_RSI_600519_20260731/统一SMA基线回放.html`

该修复解决了页面自行重算哨兵价的问题；页面是否与浏览器当前打开的旧报告一致，需要重新打开上述新生成报告验证，旧 HTML 不会自动更新。
