# 独立审计报告（2026-07-30）

## 审计范围与限制

本轮按独立审计模式执行，只进行代码阅读、Git 检查、测试、小规模回测、生成文件检查和最小复现；审计报告完成后，用户要求继续收口，随后已实施严格模式配置、逐笔账务审计和异常代码改动。原有未提交修改全部保留。

记录基线：

- 分支：`adaptive-phase1-infrastructure`
- 最新提交：`43dceee 增加Alpha与RSI哨兵独立实验门控`
- `pytest -q`：命令不存在（`zsh: command not found: pytest`）。
- `python3 -m pytest -q`：审计开始基线为 `240 passed in 19.55s`；审计结束复跑为 `243 passed in 19.78s`（工作区已有额外测试文件被收集）。
- 工作区在审计开始前已存在修改，详见审计开始时的 `git status` 和 `git diff`；本报告未重置或覆盖。

## 结论总览

| 项目 | 结论 | 证据 |
|---|---|---|
| 严格时序 | **已切换并通过新回归** | 正式配置已切换为 `precomputed_stop_entry`，核心模块“下一根执行”启用、“本根形成立即成交”关闭；严格模式真实 600118 回测的审计目录 `/tmp/strict-final.wzeBVp/result_运行` 为 `PASS`。旧版 `same_bar_entry` 结果仍只能作为历史对照。 |
| 688/689、北交所、部分卖出、停牌、涨跌停 | **部分通过，整体尚未证明** | `tests/test_execution_timing.py` 覆盖 688/北交所整手和停牌/涨跌停函数；真实 688001、689009 回测有数据并成交。但没有北交所真实行情回测；689 的涨跌停函数按当前代码落到默认 10%，是否符合业务口径尚未证明。 |
| 逐笔账务对账 | **已通过本地真实回测** | 新增 `审批记录.csv`、审批现金/持仓前后快照，以及 `trade_ledger`、`approval_trade_reconciliation`、`approval_cash_ledger`、`approval_position_ledger`。严格 600118 真实回测四项均 `PASS`；成功成交误生成 `rejection_id` 的问题也已修复。 |
| 多股同一时刻竞争资金撮合稳定性 | **已观察到稳定，但尚未形成自动化充分证明** | 运行 `600118,600519` 与反向 `600519,600118`，区间 `2025-01-01` 至 `2026-07-03`，资本 `200000`；审批记录数均 `221`，实际成交 `102`，期末现金/权益均 `204540.34623`，审批序列 SHA-256 均为 `be5236...098da0d5`。现有测试主要验证优先级函数和持仓上限，未覆盖更广泛并列事件。 |
| 零交易、哨兵未形成、数据不足诊断 | **部分通过** | 单股零交易结果新增 `异常诊断.ZERO_TRADES` 原因计数；组合数据不足错误新增 `错误代码=INSUFFICIENT_DATA`。尚未覆盖所有零交易原因的端到端分类验收。 |
| 严格历史股票池样本外验证 | **尚未证明** | `数据模块/历史成分股.py` 和 `tests/test_historical_constituents.py` 只证明快照查询函数；组合入口确实在 `运行多股回测` 中传入历史成分池，但本轮没有成功运行完整历史成分股票池样本外回测并核对逐笔结果。 |

## 关键证据

### 1. 正式策略仍存在前视风险

- 文件：`1_策略配置/参数配置.yaml`，`买入参数.买入时机模式: same_bar_entry`。
- 文件：`时序审计/trade_timing_audit.py`，函数 `审计回测` 将 `same_bar_entry` 标记为旧版模式并设置 `时序前视风险=True`。
- 命令：

  ```text
  python3 - <<'PY'
  from 回测引擎.backtest_engine import 跑回测
  r=跑回测('600118','2025-01-01','2026-07-03',静默=True)
  print(r['时序审计'])
  PY
  ```

- 结果：真实 600118 回测买入 51 笔、卖出 51 笔；`时序审计` 为 `{'结论': '仅供对照', '标准化买入时机': 'same_bar_entry', '时序前视风险': True, 'OHLC边界违规成交数': 0, '买入样本数': 51, ...}`。
- 第一笔异常：股票 `600118`，日期 `2025-01-07`，`execution_id=X00000001`；交易决策记录含 `买入时机模式: same_bar_entry`。因此不能把当前正式配置结果当作严格无前视基线。
- 已有局部测试：`tests/test_strict_entry_model.py`、`tests/test_execution_timing.py` 共 49 项定向测试通过，但没有证明正式配置已切换到严格模式。

### 2. 交易单位与涨跌停边界

- 文件：`策略引擎/规则执行器.py`，`_最低交易单位`、`_计算卖出股数`、`_获取涨跌停比例`、`_行情不可交易`、`_封死涨停`、`_封死跌停`。
- 已通过测试：`tests/test_execution_timing.py::test_market_lot_rules_are_shared_by_buy_and_partial_sell` 验证 `688001=200`、`BJ_430001=300`、主板 `=100`，并验证部分卖出取整；同文件验证停牌、封死涨停、封死跌停和创业板历史比例。
- 真实回测：
  - `688001`，`2024-01-01` 至 `2026-07-03`，数据 `2416` 根，买入 `81`、卖出 `81`。
  - `689009`，同区间，数据 `2416` 根，买入 `81`、卖出 `79`。
- 尚未证明：没有本地北交所行情文件（例如 `430001/830001` 不存在），因此北交所真实成交、停牌和涨跌停链路未被真实回测验证。当前 `_获取涨跌停比例` 对 `689` 不单独分支，会落到配置默认 `0.10`，业务口径是否正确尚未证明。

### 3. 逐笔账务审计缺口

- 文件：`运行程序/可信度审计.py`，函数 `审计运行目录`。
- 当前检查包括：运行清单、交易文件、生命周期 ID、重复 `execution_id`、成交价格、费用字段、权益曲线守恒、OHLC 价格范围。
- 未检查：
  - `审批记录` 的每一笔批准/拒绝是否都有对应 `intent_id`；
  - 成交金额、费用与现金变化是否逐笔相等；
  - 成交数量与持仓数量变化是否逐笔相等；
  - 卖出净额与账户现金增加是否逐笔相等。
- 真实运行证据目录：`/tmp/cash-audit2.../result_运行`，包含 `审批记录.csv`；审批成交总股数与交易明细总股数均为 `643800`，四项逐笔审计均为 `PASS`。

### 4. 多股撮合顺序

- 文件：`组合回测/统一多股执行器.py`，`_会话优先级`、`运行共享账户回测`。
- 反向股票池最小复现：
  - `[600118,600519]` 与 `[600519,600118]`；
  - 区间 `2025-01-01` 至 `2026-07-03`；
  - 初始资金 `200000`，流动性上限 `0.01`；
  - 两次审批均 `221`，实际成交均 `102`，现金和权益均 `204540.34623`；
  - 审批序列哈希相同：`be5236cbc158222779a357f0ae8f8cfb2f1ac7ea5914af9f27ccaf9d098da0d5`。
- 结论：该样本证明了当前两个股票顺序下结果稳定，但尚未覆盖同一时间同优先级、多股票部分成交、不同资金约束组合等全部边界。

### 5. 异常诊断

- 文件：`回测引擎/backtest_engine.py`，`准备回测数据`；`组合回测/统一多股执行器.py`，数据加载错误分支。
- 最小复现：`600118`，`2026-06-01` 至 `2026-06-10`，因过滤后少于 100 根 K 线返回 `None`；命令行入口仍生成证据目录，`可信度审计.json` 为 `WARNING`，`trade_evidence` 为 `WARNING`。
- 当前错误文案为“无可用数据或数据不足100行”，没有拆分：文件缺失、日期范围不足、指标预热不足、哨兵未形成、信号被过滤、资金拒绝、停牌等原因。
- 零交易真实回测可产生结果，但没有独立的“零交易原因分类”字段；因此只能判定有安全拦截，不能判定诊断完整。

### 6. 严格历史成分股样本外验证

- 文件：`数据模块/历史成分股.py`，`读取历史成分股`、`历史成分股池.包含`；测试：`tests/test_historical_constituents.py`。
- 文件：`运行程序/interactive_backtest_app.py`，`运行多股回测` 在勾选历史成分模式时调用 `读取历史成分股()` 并将池传入共享回测。
- 现有 CMSF 外样本文件 `10_实验记录/CMSF哨兵过滤_20260729/外样本基线.json` 的股票池为固定 5 只（`600519、000001、300750、000858、002594`），不是按每日历史成分动态形成的完整历史股票池。
- 因此严格历史股票池样本外策略结论为“尚未证明”，不能把固定 5 股结果当作历史成分无生存偏差验证。

## 审计结论

当前项目已完成正式严格预挂单切换，并新增逐笔成交、审批、现金和持仓审计；全量测试最终为 `246 passed`，严格 600118 回测运行清单和可信度审计均为 `PASS`。仍未完全收口的项目是：北交所真实数据缺失、689 涨跌停业务口径、全部撮合边界、严格历史成分股票池样本外运行，以及浏览器人工验收。

本报告完成前未修改代码、配置或数据文件。

## 继续收口验证（2026-07-30）

在报告形成后继续执行了只读验证，未修改策略逻辑、行情数据或工作簿：

- 历史成分池数据可运行：`读取历史成分股().覆盖股票("2025-01-01", "2025-12-31")` 返回 320 只；逐只调用 `回测引擎.backtest_engine.准备回测数据`，结果 `ok 320 bad 0`。因此此前“历史池全部不可用”的判断是误报，原因是之前批量命令输出截断导致尾部汇总未被正确读取。
- 历史成分模式最小真实组合回放：股票 `000001、600000`，区间 `2025-01-01` 至 `2025-03-31`，初始资金 `100000`，错误数 `0`，实际成交 `32`，期末现金 `98924.07`，持仓数量 `0`。运行参数传入 `historical_constituents`，调出成员通过 `禁止新开仓` 处理；该样本证明入口和动态资格判断已接通，但不等同于完整 320 只样本外绩效结论。
- 撮合顺序复核：输入 `[000001,600000]` 与 `[600000,000001]` 的审批序列逐项相同（32 条），实际成交均 `32`，现金均 `98924.07`；证明该样本下输入顺序不会改变结果。尚未证明所有同优先级、资金不足、不同部分成交比例组合均稳定。
- 全量测试：`python3 -m pytest -q` 输出 `246 passed in 21.93s`。测试通过不替代北交所真实行情和完整历史池样本外运行证据。

### 当前仍未收口

1. 北交所真实行情文件缺失，无法证明 `430/830` 真实成交链路；`689` 涨跌停比例业务口径仍需明确来源。
2. 需要一组专门的组合撮合边界自动化测试，覆盖同一时间同优先级、资金不足一手、允许/禁止部分成交、停牌和涨跌停组合。
3. 需要完整 320 只历史成分股票池的样本外策略运行及逐笔审计汇总，当前仅证明数据准备和小规模入口运行。
4. 页面浏览器人工验收（主页面与多股回放台）尚未执行，页面数据来源的端到端可视化证据仍为“尚未证明”。

## 四项任务继续验证（2026-07-30）

- **689 涨跌停口径：已完成。** `策略引擎/规则执行器.py::_获取涨跌停比例` 现将 `688`、`689` 统一视为科创板 20%；`tests/test_execution_timing.py` 新增 `689009` 回归断言，定向测试结果为 `37 passed`（含执行与组合边界测试）。该结论是当前项目采用的业务口径，若后续接入特殊处理股票仍需单独建模。
- **组合撮合边界：已完成当前可自动化范围。** `tests/test_shared_account_limits.py` 已覆盖整手单位、部分成交、现金非负、持仓上限和反向股票输入；新增反向输入审批序列一致性断言。定向测试 `37 passed`，真实两股票回放正序/反序审批序列逐项一致、现金均 `98924.07`。尚未覆盖真实北交所行情下的撮合。
- **320 只历史成分池样本外回测：并集运行已完成，严格资格约束运行尚未证明。** 运行目录 `/tmp/h320_运行`，区间 `2025-01-01` 至 `2025-12-31`，股票数 `320`，交易 `16083`（买入 `8089`、卖出 `7994`），审批记录 `28843`；`可信度审计.json` 为 `PASS`，六项逐笔/时序检查全部 `PASS`。该运行使用 320 只历史成分并集，但没有把 `historical_constituents` 参数写入 CLI 入口，因此不能宣称无生存偏差已完成；带动态资格约束的完整运行在本轮因输出/运行时限制未生成可审计目录，结论仍为“尚未证明”。
- **浏览器端到端验收：部分完成，发现配置展示不一致。** 启动 `python3 运行程序/interactive_backtest_app.py --host 127.0.0.1 --port 8765 --no-open`，访问 `http://127.0.0.1:8765`；DOM 同时出现“回测主页面”和 iframe 内“多股策略决策回放台”，结果目录、实际成交、组合权益、股票池、资金模式和回放股票链接均可见，数据来自交互运行目录而非页面推算。但页面当前摘要同时显示“首次开仓时机：本根形成哨兵价后立即买入”和配置列表中“下一根K线执行”，与正式严格时序配置不一致；因此页面端到端验收只能判定为“数据可追溯通过、配置展示一致性尚未证明”。
- **浏览器端到端验收：数据链路通过，配置展示修复后待重新加载确认。** 已将 `构建默认表单` 的严格模式回退默认值改为 `precomputed_stop_entry`，并通过 `tests/test_interactive_backtest_app.py` 等定向测试 `65 passed`。原浏览器快照中的矛盾来自旧页面运行状态；需重启/刷新页面后再做最终人工确认，当前仍不能把旧快照当作最终通过证据。

## 继续验证（2026-07-30）

- 动态历史成分完整运行已实际完成：320 只、2025-01-01 至 2025-12-31、972 个时间点、错误数 0、审批 28,843 条、实际成交 16,083 笔；`historical_constituents` 资格约束已传入执行器，拒绝订单 12,760 条（含资金不足一手 8,728 条、现金/仓位限制 4,014 条）。期末权益 17,803,545.08 元，现金 3,553,628.08 元，持仓市值 14,249,917 元。该运行输出保存于 `/tmp/h320_dynamic_summary.json`，但本次脚本未将内存审批与交易明细落成标准证据目录，因此逐笔可信度审计仍未完成，不能标记为完整审计通过。
- 页面刷新复核未完成：原浏览器会话已关闭，刷新调用返回“Existing tabs: none”。代码默认值修复和 65 项定向测试通过，但页面最终人工刷新证据仍为“尚未证明”。
