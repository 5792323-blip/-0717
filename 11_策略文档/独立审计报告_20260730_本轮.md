# 独立审计报告（2026-07-30 本轮）

## 审计范围与基线

- 工作目录：`/Users/chen/Desktop/策略0717`
- 分支：`adaptive-phase1-infrastructure`
- 最新提交：`1a80d2f 记录买入时机配置变更影响`
- 工作区：存在大量未提交修改和两个未跟踪文件；本轮未删除、覆盖、重置或修改既有文件。
- `pytest -q`：失败，`zsh: command not found: pytest`。
- 等价命令 `python3 -m pytest -q`：`247 passed in 21.32s`。

## 主要发现（按风险排序）

### [高] 页面实际工作配置仍是旧的同根成交模式

- 证据文件：`10_实验记录/当前回测工作台配置.json`，字段 `single_entry_timing` 和 `multi_entry_timing` 均为 `same_bar_entry`。
- 正式配置：`1_策略配置/参数配置.yaml` 的首次开仓模式为 `precomputed_stop_entry`；`1_策略配置/核心模块配置.yaml` 启用“下一根执行”。
- 页面代码：`运行程序/interactive_backtest_app.py:2281` 的 `读取最近工作台配置()` 会加载持久化工作台配置；`运行程序/interactive_backtest_app.py:2483` 只在构建默认表单时读取正式配置，不能覆盖已保存的旧工作台配置。
- 浏览器证据：访问 `http://127.0.0.1:8766` 后读取页面 DOM，`single_entry_timing=same_bar_entry`、`multi_entry_timing=same_bar_entry`；同时页面存在“回测主页面”和“多股策略决策回放台”。
- 结论：正式策略配置已严格，但页面当前可提交的工作配置仍可能运行旧模式。页面配置与策略核心不一致，尚未证明页面回测使用严格时序。

### [中] 逐笔账务审计对小规模 CLI 回测通过，但审计器存在覆盖边界

- 命令：`python3 运行程序/run_backtest.py --stocks 600118,600519 --start 2025-01-01 --end 2025-03-31 --capital 200000 --liquidity-limit 0.01 --output /tmp/audit_small.json`
- 证据目录：`/tmp/audit_small_运行`。
- 文件存在：`运行清单.json`、`回测结果.json`、`交易明细.csv`（16 行）、`审批记录.csv`（42 行）、`可信度审计.json`。
- 审计结果：`PASS`；`trade_ledger`、`approval_trade_reconciliation`、`approval_cash_ledger`、`approval_position_ledger`、`cash_conservation`、`lookahead` 全部 `PASS`。
- 代码证据：`运行程序/可信度审计.py:74-137` 逐笔检查交易数量、价格、费用、库存、审批现金和审批持仓；`运行程序/run_backtest.py:347-362` 保存交易和审批证据。
- 限制：`trade_ledger` 按保存文件行顺序重建库存，未按时间戳/交易编号主动排序；本次样本通过不能证明乱序文件或跨文件合并场景也正确。

### [中] 历史成分资格逻辑已接入，但完整动态运行未形成标准逐笔证据目录

- 代码证据：`组合回测/统一多股执行器.py:319-322` 按时间点调用 `历史成分股池.包含()` 并设置 `禁止新开仓`；`数据模块/历史成分股.py:46-62` 实现快照查询和覆盖股票并集。
- 动态运行摘要：`/tmp/h320_dynamic_summary.json`，320 只、2025-01-01 至 2025-12-31、972 时间点、错误数 0、审批 28,843、实际成交 16,083。
- 但该运行只保存摘要，没有标准 `运行清单.json`、`交易明细.csv`、`审批记录.csv` 和 `可信度审计.json`，因此逐笔动态成分审计尚未证明。

### [中] 北交所真实行情链路尚未证明

- 检查结果：`数据模块/raw/430001_双价格合并.pkl`、`830001_双价格合并.pkl`、`872925_双价格合并.pkl` 均不存在；`920000` 文件存在但不能代表完整北交所样本。
- 代码：`策略引擎/规则执行器.py:2488-2502` 对 `4/8/9` 代码使用 30% 涨跌停和 300 股整手规则。
- 结论：本地没有足够的北交所真实行情，停牌、涨跌停和成交链路尚未证明。

## 已通过项目

- 严格时序单股回测：命令调用 `跑回测('600118','2025-01-01','2025-03-31',初始资金=200000,静默=True)`；交易 16 笔；`时序审计` 结论为“通过”，标准化模式 `precomputed_stop_entry`，前视风险 `False`，OHLC 边界违规 `0`。第一笔交易股票 `600118`，具体日期和交易编号未在本次摘要中单独提取，不能据此声称第一笔逐笔时序已完整证明。
- 数据不足最小复现：`准备回测数据('600118','2026-06-01','2026-06-10','1_策略配置')` 返回 `None`，现有错误粒度仍主要是“数据不足100行”；零交易原因分类的完整端到端行为尚未证明。
- 689 口径代码为 688/689 统一 20%，且 `tests/test_execution_timing.py` 有 `689009` 断言；本轮未修改代码，结论仅基于既有工作区内容。
- 页面数据来源：`运行程序/interactive_backtest_app.py:1600-1615` 使用 iframe 加载报告/多股/单票回放；页面 DOM 可见“回测主页面”和“多股策略决策回放台”，但因工作配置旧值，页面端到端严格时序仍未证明。

## 审计结论

当前不能宣布“策略优化、页面、运行契约和数据保存管理已完整正确”。自动化测试和小规模 CLI 证据链通过，但存在一个高风险配置一致性问题：正式配置为严格预挂单，持久化页面工作配置仍为 `same_bar_entry`。此外，动态历史成分运行未形成标准逐笔审计目录，北交所真实行情缺失，均应标记为“尚未证明”。

## 建议（不属于本轮实施）

1. 明确页面启动时正式配置与已保存工作配置的优先级，并为工作配置增加配置版本/时序模式校验。
2. 将动态历史成分运行接入标准证据保存和可信度审计入口。
3. 补充真实北交所行情后，再验证 430/830/920 交易单位、涨跌停和停牌边界。
