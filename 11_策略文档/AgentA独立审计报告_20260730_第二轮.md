# Agent A 独立审计报告（第二轮）

项目：`/Users/chen/Desktop/策略0717`  
审计模式：只读；未修改代码、配置、测试或其他 agent 的未提交文件。  
审计运行编号：`agentA_多股_运行`

## 1. 总体结论

当前实现已经真实生成运行清单、交易明细、审批记录和可信度审计。多股样本中 254 笔成交、254 个唯一成交编号、43 条组合层拦截，资金/持仓/费用/价格/未来数据和审批账务检查均为 `PASS`。

但不能判定需求“完整落实”：

- CLI 运行清单没有 `experiment_id`；页面入口与 CLI 身份契约不一致。
- 审批记录没有 `intent_id`、`order_id`，不能完成“意图→审批→订单→成交”的完整关联证明。
- 页面主要显示汇总和个股回放 iframe，尚未证明点击单笔成交/拒绝可显示完整审批前后账户变化。
- 当前未提交配置已改变正式交易行为，原始基线不能直接用于当前配置。
- 深度数据清理没有影响预览和删除后明确降级提示的端到端证据。

结论：**不允许仅凭当前测试和样本 PASS 进入下一阶段；需先冻结并行修改并补齐上述证据。**

## 2. 开始前记录

执行：`git status; git branch --show-current; git log -1 --oneline; git diff --stat; git diff; python3 -m pytest -q`

- 分支：`adaptive-phase1-infrastructure`
- 最近提交：`1a80d2f 记录买入时机配置变更影响`
- 工作区存在多个未提交修改，包括配置、执行器、页面、测试和审计工具；均未触碰。
- 测试实际结果：`246 passed in 21.19s`。

## 3. 多股运行证据

命令：

```text
python3 运行程序/run_backtest.py --config 1_策略配置 \
  --stocks 600519,000001 --start 2020-01-02 --end 2021-12-31 \
  --capital 2000000 --workers 1 --portfolio-mode shared \
  --output /tmp/agentA_多股.json
```

目录：`/tmp/agentA_多股_运行`

- 文件：`运行清单.json`、`回测结果.json`、`交易明细.csv`、`审批记录.csv`、`可信度审计.json`。
- 成交：254 笔（买入 127、卖出 127）。
- 股票成交数：600519 为 104，000001 为 150。
- 审批记录：实际成交 254 条，组合层拦截 43 条。
- 生命周期唯一性：`intent_id/order_id/execution_id` 均为 254 个唯一值。
- 可信度：`PASS`；`trade_ledger`、`approval_trade_reconciliation`、`approval_cash_ledger`、`approval_position_ledger`、`cash_conservation`、`lookahead` 均为 `PASS`。
- 部分成交：0 条，结论为 `NOT_RUN`，不是“通过”。

## 4. 需求—代码—数据—页面映射

| 需求 | 代码/函数 | 运行字段/文件 | 页面 | 等级 | 结论 |
|---|---|---|---|---|---|
| `run_id` 唯一 | `interactive_backtest_app.py::创建运行目录` | 时间戳目录、`运行清单.json` | 首页恢复目录 | 已部分落实 | 未做并发碰撞证明 |
| 完整 `experiment_id` | `interactive_backtest_app.py::写入运行清单` | 页面入口有 16 位实验哈希 | 页面未展示并未由 CLI 生成 | 已部分落实 | CLI 清单无该字段；费用/数据内容纳入不完整 |
| 交易生命周期编号 | `策略引擎/交易记录器.py`、`run_backtest.py` | 交易明细有三类成交编号 | 个股回放消费交易明细 | 已部分落实 | 成交有效；审批没有 intent/order 关联 |
| 拒绝订单 | `交易账户.py::记录审批`、`统一多股执行器.py` | 43 条 `rejection_id`、分类、详细原因 | 主页面拒绝汇总/明细接口 | 已部分落实 | 拒绝可追溯；页面逐笔账户链路尚未证明 |
| 唯一事实来源 | `规则执行器.py`、`交易账户.py`、`统一多股执行器.py` | 成交、审批前后现金/持仓 | 页面消费保存结果 | 已完整落实（样本） | 当前样本逐笔审计 PASS |
| 页面不重算成交事实 | `回测引擎/report_generator.py::生成报告` | 持仓过程、交易明细 | 单股/个股回放 | 测试 `test_report_uses_real_holding_sentinel_without_recomputing` | 已完整落实（覆盖哨兵重算风险） |
| 可信度契约 | `运行程序/可信度审计.py::审计运行目录` | PASS/WARNING/FAIL 检查项 | 首页显示可信度状态 | `/tmp/agentA_多股_运行/可信度审计.json` | 已部分落实；`NOT_RUN/NOT_APPLICABLE` 未统一 |
| 主页面核心指标来源 | `interactive_backtest_app.py::首页` | `多股回测结果.json`、内存 live | 首页指标 | 页面测试通过 | 已部分落实；未完成逐字段自动对账 |
| 多股回放逐笔事件链 | `生成多股策略回放页面`、`查看多股单票回放` | 汇总、个股交易/持仓/行情文件 | iframe 个股回放 | 路由测试通过 | 已部分落实；没有完整事件时间线面板 |
| 数据保存/删除 | `结果生命周期.py` | 核心文件集合、锁定标志 | 生命周期接口 | 生命周期测试通过 | 已部分落实；删除影响提示尚未证明 |

## 5. 纵向追踪

### 买入成交

- 600519，2020-01-13：`600519:I00000001` → `600519:O00000001` → `600519:X00000001`；100 股，1117.20，费用 29.0472；审批现金 2,000,000 → 1,888,250.9528，持仓 0 → 100。
- 600519，2020-02-04：`600519:I00000003` → `600519:O00000003` → `600519:X00000003`；100 股，1016.01，费用 26.41626；总成本 101,627.41626。
- 600519，2020-02-11：`600519:I00000005` → `600519:O00000005` → `600519:X00000005`；100 股，1077.08，费用 28.00408；买入成本守恒通过。

### 卖出成交

- 600519，2020-01-14：`600519:I00000002` → `600519:O00000002` → `600519:X00000002`；100 股，1114.84，费用 140.46984，净额 111,343.53016。
- 600519，2020-02-10：`600519:I00000004` → `600519:O00000004` → `600519:X00000004`；100 股，1059.12，费用 133.44912，净额 105,778.55088。
- 600519，2020-02-14：`600519:I00000006` → `600519:O00000006` → `600519:X00000006`；100 股，1085.69，费用 136.79694，净额 108,432.20306。

### 拒绝订单

- 600519，2021-01-07，`R00000001`：资金不足一手；分类“策略或单只仓位上限”；现金 2,046,671.56944 前后不变，持仓 0 → 0。
- 600519，2021-01-08，`R00000002`：资金不足一手；分类“策略或单只仓位上限”；现金和持仓均不变。

部分审批/部分成交：本运行没有记录，状态 `NOT_RUN`。

## 6. 页面检查

代码和测试证据表明：

- 首页从 `多股回测结果.json`、`可信度审计.json` 恢复多股汇总和可信度。
- 多股回放页面由 `summary`、`details`、`组合权益曲线` 生成。
- 个股回放从同一运行目录读取 `回放行情.csv.gz`、`交易明细.csv`、`持仓过程.csv`。
- 主页面和回放路由相关测试通过；真实本地 HTTP 首页曾返回 200。

尚未证明：浏览器点击某笔成交或拒绝后能否显示审批、订单、成交、费用、现金和持仓的完整链路；删除深度数据后页面是否明确降级提示。

## 7. 原有策略保护

当前配置修改已改变正式行为。对照证据：

- Git 基线第一笔：600519，2020-02-04，RSI上穿20；217 笔交易；最终权益 20,033,605.63476。
- 当前配置第一笔：600519，2020-01-13，RSI上穿70；267 笔交易；最终权益 20,029,510.02934。

这是已生效的时序/配置实验，不是页面数据契约变化。当前配置下的新基线尚未冻结。

## 8. 最严重的五项风险

1. 并行配置改变正式交易行为，旧基线不能继续代表当前策略。
2. CLI 与页面 `experiment_id` 不一致，实验复现身份不完整。
3. 审批记录缺少意图和订单编号，生命周期无法完全前向追踪。
4. 页面以汇总和 iframe 为主，逐笔审批/账户变化可视化链路尚未证明。
5. 无交易、部分成交、深度数据删除等状态未形成统一的 `NOT_RUN/NOT_APPLICABLE` 展示契约。

## 9. 建议新增测试

- CLI 与页面同一实验生成相同 `experiment_id`，并覆盖费用、数据、股票池和账户模式变更。
- 每条实际成交审批记录必须包含并关联 `intent_id`、`order_id`、`execution_id`。
- 页面指标与结果 JSON、审批 CSV、可信度 JSON 逐字段对账。
- 构造真实部分成交，验证页面和审计显示为 `PARTIAL`，而非成交或拒绝。
- 删除深度数据后验证页面明确提示“无法完整重放决策链”。
- 并发运行目录和实验身份碰撞测试。

## 10. 准入结论

**暂不允许进入下一阶段。**

在冻结其他 agent 的配置和代码修改、补齐实验身份、审批生命周期关联、页面逐笔展示和保存分层降级提示前，不能宣称策略优化、主页面、多股回放台和运行数据契约已完整落实。
