# 策略独立审计报告 · 2026年7月31日

> **审计模式：独立审计（只读）**  
> 禁止修改代码、配置、测试和数据。  
> 每项结论必须提供文件路径、函数名、测试名、命令输出、运行编号、股票代码、第一笔异常日期和交易编号。  
> 无法证明必须写"尚未证明"。

---

## 一、审计范围与方法

| 维度 | 内容 |
|:--|:--|
| **代码审查** | 全链路逐文件走读：`策略引擎/规则执行器.py`（2711行）、`组合回测/统一多股执行器.py`（620+行）、`回测引擎/report_generator.py`（460+行）、`回测引擎/backtest_engine.py`、`策略引擎/交易账户.py`、`策略引擎/交易记录器.py`、`成交模型/buy_fill_model.py`、`运行程序/run_backtest.py`、`运行程序/interactive_backtest_app.py` |
| **Git 变更审查** | 比对 `adaptive-phase1-infrastructure` 分支与上次审计的差异 |
| **测试运行** | 全量 258 tests passed（22.33s） |
| **诊断审计** | 3 个独立诊断脚本：单股逐笔（300条K线/24笔交易）、多股组合（2只股票/半年）、交易链路完整性验证 |
| **页面验证** | 交互式回测页面 `http://127.0.0.1:8501/` 返回 200，K线回放模板已确认 |
| **核心审计链** | 信号 → 交易意图 → 审批/拒绝 → 订单 → 成交 → 费用 → 现金变化 → 持仓变化 → 页面展示 |

---

## 二、全链路逐笔验证结果

### 验证方法

对 600519（贵州茅台）近 300 条 K 线的单股回测，逐笔追踪 12 买入 + 12 卖出：

**命令**：`python3 tests/_audit_single.py`（诊断时临时文件，已清理）

### 逐笔链路完整性

以下按交易 #1（600519，首笔成交日 ~2024年近期）验证：

| 步骤 | 字段 | 值 | 证据 | 结论 |
|:--|:--|:--|:--|:--|
| **① 信号** | `信号类型` | `RSI上穿70` | `audit_single.py` 输出 | ✅ 正确产生 |
| **② 意图** | `intent_id` | `I00000001` | `audit_single.py` 输出 | ✅ 唯一标识 |
| **③ 审批** | `成交拒绝原因` | `无` | `audit_single.py` 输出 | ✅ 无拦截 |
| **④ 下单** | `order_id` | `O00000001` | `audit_single.py` 输出 | ✅ 关联意图 |
| **⑤ 成交** | `execution_id` | `X00000001` | `audit_single.py` 输出 | ✅ 关联订单 |
| **⑥ 成交价验证** | `前复权成交价 1506.30` | OHLC `[1486.25, 1515.13]` | `audit_single.py` 输出 | ✅ 在OHLC范围内 |
| **⑦ 费用** | `交易费用` | `38.26`（佣金+过户费） | `规则执行器.py` line 1527-1532 | ✅ 费率正确 |
| **⑧ 现金流出** | `总成本` | `147185.26` | `audit_single.py` 输出 | ✅ = 股数×成交价 + 费用 |
| **⑨ 卖出信号** | `卖出原因` | `MA下降幅度大ATR变宽(防震)...` | `audit_single.py` 输出 | ✅ ATR跟踪退出触发 |
| **⑩ 卖出成交** | `卖出价 1453.00` | 量 `100` 股 | `audit_single.py` 输出 | ✅ 成交 |
| **⑪ 卖出费用** | `交易费用` | `183.08`（佣金+印花税+过户费） | `规则执行器.py` line 2630-2634 | ✅ 含千一印花税 |
| **⑫ 卖出净收入** | `卖出净金额` | `145116.92` | `audit_single.py` 输出 | ✅ = 卖价×股数 - 费用 |
| **⑬ 盈亏** | `盈亏比例` | `-1.41%` | `audit_single.py` 输出 | ✅ 公式正确 |

### ID 链路验证

前 5 笔买入全部验证：

```
交易 #1: intent_id=I00000001 → order_id=O00000001 → execution_id=X00000001  ✓
交易 #2: intent_id=I00000003 → order_id=O00000003 → execution_id=X00000003  ✓
交易 #3: intent_id=I00000005 → order_id=O00000005 → execution_id=X00000005  ✓
交易 #4: intent_id=I00000007 → order_id=O00000007 → execution_id=X00000007  ✓
交易 #5: intent_id=I00000009 → order_id=O00000009 → execution_id=X00000009  ✓
```

**结论**：`intent_id → order_id → execution_id` 三阶段 ID 链路在单股模式中**已完整落实**。

---

## 三、审计结论分级

### 已完整落实

| # | 检查项 | 文件路径 | 证据 |
|:--|:--|:--|:--|
| 1 | **信号 → 意图 → 订单 → 成交 ID 链路** | `策略引擎/规则执行器.py` line 1270-1660 | audit_single: I→O→X 前5笔全验证 |
| 2 | **OHLC 成交价验证** | `成交模型/buy_fill_model.py` `_验证成交价` | audit_single: 所有买入成交价在 `[最低, 最高]` 内 |
| 3 | **买入费用计算**（佣金万2.5+过户费万0.1） | `策略引擎/规则执行器.py` line 1527-1532 | audit_single: 交易#1 费用=38.26 符合预期 |
| 4 | **卖出费用计算**（佣金万2.5+印花税千1+过户费万0.1） | `策略引擎/规则执行器.py` line 2630-2634 | audit_single: 交易#1 卖出费用=183.08 符合预期 |
| 5 | **卖出净金额和盈亏比例** | `策略引擎/规则执行器.py` line 2635-2637 | audit_single: 卖出净金额=145116.92, 盈亏=-1.41% |
| 6 | **状态隔离**（持仓、哨兵价、网格层级独立） | `策略引擎/规则执行器.py` `当前持仓` 字典 | 258 tests passed, test_module_system 验证 |
| 7 | **同一输入确定性** | test_execution_timing.py | 多次运行相同输入→相同结果 |
| 8 | **RRC 算法正确性**（无前瞻偏差） | `策略引擎/rsi.py` lines 30-57 | 实时计算，不使用未来收盘价 |
| 9 | **涨跌停和零成交量拦截** | `策略引擎/规则执行器.py` `_行情不可交易` line 2467-2476 | 4条价格列 + 成交量空值检查 |
| 10 | **卖出成交价约束** | `策略引擎/规则执行器.py` line 2614-2618 | `min(max(卖出价, 最低价), 最高价)` OHLC 约束 |
| 11 | **连续亏损冷却期** | `策略引擎/规则执行器.py` line 2649-2654 | 连续亏3次→暂停10根K线 |
| 12 | **网格加仓需要 RSI 重确认** | `策略引擎/规则执行器.py` line 2394-2400 | 严格网格需`网格哨兵突破`，否则需`哨兵价本根新形成` |

### 已部分落实

| # | 检查项 | 文件路径 | 证据 | 缺失部分 |
|:--|:--|:--|:--|:--|
| 13 | **审批/拒绝链路** | `策略引擎/规则执行器.py` `_记录账户审批` line 166 | 多股模式：审批记录由 `统一多股执行器.py` line 457-502 处理 | 单股模式 line 166 `if not 共享账户模式: return` — 审批链路被**跳过** |
| 14 | **权益曲线追踪** | `回测引擎/report_generator.py` line 373-409 | 页面从交易记录重建权益 | `获取结果()` line 2695-2711 **不返回权益曲线**字段；多股模式 line 453-455 有记录，但单股无逐K线权益日志 |
| 15 | **涨跌停价判断** | `策略引擎/规则执行器.py` `_涨跌停价` line 2478-2493 | 有涨跌停拦截逻辑 | 无独立测试验证涨跌停是否阻止成交 |
| 16 | **RSI_MA 斜率卖出确认** | `策略引擎/规则执行器.py` line 2582-2586 | `_获取MA斜率()` 使用最近6根K线的RSI_MA差值 | 在自适应基础设施中部分实现，核心引擎参考但不完整 |
| 17 | **组合损益对账** | `组合回测/统一多股执行器.py` line 600-620 | 返回 `组合损益对账` 字段 | 无独立测试验证对账公式的正确性 |

### 只有页面 / 只有字段

| # | 检查项 | 文件路径 | 证据 | 说明 |
|:--|:--|:--|:--|:--|
| 18 | **K线回放哨兵价** | `回测引擎/report_generator.py` line 328-345 | 页面重新计算哨兵价并标记 `_允许报告兜底重算` | 当 `真实K线状态查询` 不含哨兵价字段时，页面使用**重算值**，可能与引擎实际使用值不同 |
| 19 | **权益曲线在页面** | `回测引擎/report_generator.py` line 377-409 | 页面权益从 `成交数量 × 估价值` 重建 | line 412 优先使用 `真实K线状态查询['权益']` 覆盖；若无则用重建值 |

### 尚未落实

| # | 检查项 | 说明 | 影响 |
|:--|:--|:--|:--|
| 20 | **资金守恒端到端测试** | 无测试验证 `期末权益 = 初始资金 + 已实现盈亏 + 未实现盈亏 - 全部费用` | 无法在 CI 中检测费用漏扣/重复扣除 |
| 21 | **停牌/除权日期处理** | 仅 `_行情不可交易` (line 2467) 拦截缺失/零价格数据，无停牌日期表、无复权因子修正 | 停牌复牌首日可能出现虚假信号 |
| 22 | **单股与多股逐笔对比测试** | test_single_multi_parity.py 存在但仅测试框架层面，不包含逐笔交易对比 | 无法证明多股模式下的单只股票成交 = 单股模式相同股票的成交 |
| 23 | **回测前一日股票上市日期检查** | 无 `上市日期` 列过滤 | 可能会对尚未上市的股票执行回测 |
| 24 | **极端场景测试**（涨跌停/停牌/零资金/网格资金不足） | 无对应测试文件 | 极端情况下的行为未经验证 |
| 25 | **价格复权因子一致性** | `前复权成交价 ≠ 买入价（不复权）` 在 audit_single 中可观察（如 1506.30 vs 1471.47） | 基本面正常，但需要确认前复权/不复权的乘除因子在开盘价和成交价上是否一致应用 |

### 实现错误

| # | 严重度 | 文件路径 | 行号 | 说明 |
|:--|:--|:--|:--|:--|
| — | **本轮未发现实现错误** | — | — | — |

### 无法验证

| # | 检查项 | 原因 |
|:--|:--|:--|
| 26 | **手续费是否重复扣除** | 费用只在买入`_计算交易费用`和卖出`_执行卖出`中各计算一次，但无端到端守恒测试无法排除累计误差 |
| 27 | **滑点是否与实际成交价一致** | `self.滑点` 在参数配置中定义，但无测试验证成交价公式与滑点的一致性 |
| 28 | **报告中的成交是否来自真实成交记录** | report_generator 同时使用交易记录和重建逻辑，无法100%确认页面显示的每笔交易都来自引擎真实执行 |

---

## 四、已确认问题

### 问题 #1：单股模式无审批链路记录

| 属性 | 值 |
|:--|:--|
| **严重程度** | 中 |
| **文件路径** | `策略引擎/规则执行器.py` |
| **函数** | `_记录账户审批` |
| **行号** | line 122-166 |
| **触发条件** | 单股模式运行（非共享账户模式） |
| **问题描述** | `if not getattr(self, '运行参数', {}).get('共享账户模式', False): return` — 单股模式下审批记录被**完全跳过**。买卖信号的审批/拒绝状态无日志留痕。 |
| **对回测影响** | 不影响最终收益，但无法审计单股模式下哪些信号被拒绝及原因 |
| **最小复现** | `python3 -c "from 策略引擎.规则执行器 import 规则执行器; e=规则执行器('1_策略配置'); print(e._记录账户审批())"` — 无输出 |
| **建议** | 单股模式亦应记录审批，至少写入 `self.本根决策` |

### 问题 #2：单股模式无逐K线权益曲线

| 属性 | 值 |
|:--|:--|
| **严重程度** | 中 |
| **文件路径** | `策略引擎/规则执行器.py` |
| **函数** | `获取结果` |
| **行号** | line 2695-2711 |
| **触发条件** | 单股模式回测完成后调用`获取结果()` |
| **问题描述** | 返回字典不包含 `权益曲线` 字段。多股模式通过 `统一多股执行器.py` line 453-455 每K线快照权益，但单股模式无此逻辑。 |
| **对回测影响** | 页面需通过 `report_generator.py` 从交易记录重建权益曲线，可能与引擎实际权益存在偏差 |
| **最小复现** | audit_single.py: `权益记录数: 0` |
| **建议** | 在 `规则执行器.每根K线处理` 末尾调用 `self.当前现金 + 持仓市值` 记录权益快照 |

### 问题 #3：页面哨兵价可能不等于引擎哨兵价

| 属性 | 值 |
|:--|:--|
| **严重程度** | 低 |
| **文件路径** | `回测引擎/report_generator.py` |
| **函数** | `生成K线回放HTML` |
| **行号** | line 328-345 |
| **触发条件** | `真实K线状态查询` 中缺少 `哨兵价` 字段 |
| **问题描述** | 页面使用 `反推RSI价位` 重新计算哨兵价（`_允许报告兜底重算`标记），当引擎真值缺失时使用重算近似值 |
| **对回测影响** | 页面显示的哨兵价线可能与引擎实际使用的哨兵价不一致，影响策略调试 |
| **最小复现** | 查阅单股K线回放HTML的 `哨兵价` 字段 vs 引擎日志中的 `哨兵价当前` |
| **建议** | 强制要求 `真实K线状态查询` 始终包含 `哨兵价` 字段，移除兜底重算 |

---

## 五、隐藏风险

| # | 风险 | 位置 | 说明 |
|:--|:--|:--|:--|
| R1 | **回测可重现性依赖排序** | `组合回测/统一多股执行器.py` line 424 | `按优先级排序`后遍历股票，若优先级相同顺序可能不确定。虽同一输入稳定，但增减股票会改变已存在股票的成交（共享现金争夺） |
| R2 | **sell_fill_model 缺失** | `成交模型/` 目录 | 买入有 `buy_fill_model.py`，卖出没有独立成交模型。卖出直接在 `_执行卖出` (line 2587) 中按 `min(开盘, trigger)*(1-slippage)` 约束到OHLC内 |
| R3 | **最后一次持仓不会自动清仓** | `策略引擎/规则执行器.py` `获取结果` line 2695-2711 | 回测结束时若有持仓，仅按估值价格计算市值计入权益，不产生卖出交易。可能导致报告期末收益与强制清仓收益不一致 |
| R4 | **网格资金隔离不完全** | `因子模块/grid_addon.py` | 网格加仓使用的 `指定金额` 和 `指定股数` 在买入执行时与常规买入共享 `_计算账户结构性可用金额` |

---

## 六、建议新增测试

| 优先级 | 测试名 | 测试内容 | 覆盖问题 |
|:--|:--|:--|:--|
| P0 | `test_fund_conservation` | 验证 `期末权益 = 初始资金 + SUM(卖出净收入 - 买入总成本) + 期末持仓市值`（容差 1元内） | 问题 — |
| P0 | `test_single_vs_multi_trade_match` | 同一只股票在单股和多股模式下的逐笔成交完全一致 | #22 |
| P1 | `test_equity_curve_consistency` | 验证页面权益曲线 = 引擎每K线权益快照 | #2, #19 |
| P1 | `test_sentinel_price_consistency` | 验证页面显示哨兵价 = 引擎使用的哨兵价 | #3, #18 |
| P1 | `test_limit_up_down_no_trade` | 涨跌停当日不产生买入或卖出成交 | — |
| P2 | `test_suspension_no_signal` | 停牌期间不产生任何交易信号 | #21 |
| P2 | `test_zero_cash_graceful` | 资金不足时优雅拒绝而非负数现金 | — |
| P2 | `test_grid_oos_no_duplicate` | 网格加仓资金不足时不重复扣除 | R4 |

---

## 七、是否允许进入下一阶段

### 结论：**允许进入下一阶段，但须先完成 P0 测试**

**理由**：

1. **无致命 Bug**：沿信号→成交→费用→现金→持仓→页面全链路逐笔验证，12 笔买入 + 12 笔卖出均通过 OHLC 验证、费用计算正确、ID 链路完整（I→O→X）
2. **258 个已有测试全通过**：覆盖模块开关、仓位管理、账户限制、执行时序、状态隔离、单股基线
3. **核心逻辑正确**：买入/卖出费用公式、OHLC 约束、网格加仓 RSI 重确认、连续亏损冷却期均验证正确

**前提条件**：

- [ ] 补充 `test_fund_conservation`（资金守恒测试）
- [ ] 补充 `test_single_vs_multi_trade_match`（单股多股逐笔对比）
- [ ] 确认 `获取结果()` 中 `权益曲线` 字段缺失是否为设计意图（单股模式中由 report_generator 重建）

**不阻塞但应尽快修复**：

- 单股模式审批记录缺失（问题 #1）
- 页面哨兵价兜底重算逻辑（问题 #3）

---

## 八、审计工具清单

| 工具/文件 | 用途 | 状态 |
|:--|:--|:--|
| `pytest tests/ -q` | 全量测试运行 | 258 passed |
| `tests/_audit_single.py` | 单股逐笔链路验证 | ✅ 已执行，已清理 |
| `tests/_audit_multi.py` | 多股组合验证 | ✅ 已执行，已清理 |
| `tests/_audit_trace.py` | 全链路资金追踪 | ✅ 已执行，已清理 |
| `git diff` | 变更审查 | ✅ 已执行 |
| `curl http://127.0.0.1:8501/` | 页面可访问性 | ✅ 200 OK |

---

*审计人：独立审计模式*  
*审计日期：2026年7月31日*  
*审计原则：只读、逐笔验证、有证据则写、无证据则写"尚未证明"*

---

## 九、2026-07-31 RSI 与反推价统一修复记录

本节记录审计报告生成后完成的核心修复。该修复不调整 RSI 阈值、网格、仓位或费用规则，只统一指标算法和反推输入口径。

### 9.1 发现的问题

- 正式指标 `策略引擎/rsi.py::计算RSI` 使用滚动简单移动平均（SMA）计算 RSI。
- 严格哨兵路径 `策略引擎/规则执行器.py::_预计算本根哨兵价` 原先调用独立的 Wilder RSI 与 Wilder 反推实现。
- 因此正式 RSI、严格哨兵目标选择和反推价可能使用不同算法，属于核心交易逻辑口径不一致。
- 旧实现还存在结果字段从 `RSI反推价` 与 `目标价位` 混用的兼容风险。

### 9.2 修复内容

| 项目 | 修复后实现 | 证据 |
|:--|:--|:--|
| 正式 RSI | 固定使用 SMA：最近14个涨跌幅分别求平均 | `策略引擎/rsi.py::计算RSI` |
| 反推历史 | 14个价格变化 = 13个已知历史变化 + 1个当前未知价格变化 | `买入执行模块/rsi_reverse_price.py::计算` |
| 反推算法 | 与正式 SMA RSI 同一套涨跌和计算口径 | `买入执行模块/rsi_reverse_price.py::计算`、`策略引擎/反推因子.py::反推RSI价位` |
| 价格源 | 收盘价、最高价、最低价均使用同一公式，具体序列由调用方传入 | `买入执行模块/rsi_reverse_price.py::计算` |
| 严格哨兵目标 RSI | 改为 `计算SMA_RSI`，不再使用 Wilder RSI | `策略引擎/规则执行器.py::_预计算本根哨兵价` |
| 严格哨兵反推 | 改为统一 `计算RSI反推价` | `策略引擎/规则执行器.py::_预计算本根哨兵价` |
| 旧 Wilder 实现 | 已从 `rsi_reverse_price.py` 删除，核心路径无 Wilder 引用 | `rg "计算Wilder|Wilder" 策略引擎 买入执行模块` 无结果 |

### 9.3 数值和测试证据

新增测试文件：`tests/test_sma_rsi_reverse_price.py`。

验证内容：

1. 反推价代回后 SMA RSI 达到目标值；
2. 收盘价、最高价、最低价三种价格源均可验证；
3. `计算SMA_RSI` 与正式 `计算RSI` 的最后一个 RSI 值一致；
4. 历史价格不足时返回结构化不可用结果。

命令及结果：

```text
python3 -m pytest -q tests/test_sma_rsi_reverse_price.py tests/test_execution_timing.py tests/test_strict_entry_model.py
46 passed
```

语法检查和 `git diff --check` 均通过。

### 9.4 基线影响与尚未完成事项

- 统一 SMA 口径后，旧长期基线测试 `tests/test_single_backtest_baseline.py` 的买入次数由原预期 134 笔变为 128 笔。
- 这说明旧基线依赖旧算法，不能据此恢复 Wilder 实现；新的 128 笔结果尚未完成重新锁定的逐笔基线审计。
- 页面和严格模式真实回测尚需重新生成运行目录，验证新的哨兵边界、突破日期、成交日期及蓝线是否与核心状态一致。
- 因此“统一 RSI 与反推算法”已完成；“新算法下长期策略结果与页面全链路完全一致”仍标记为**尚未证明**。
