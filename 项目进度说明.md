# 策略0717项目进度说明

最后更新：2026-07-28

## 1. 项目定位

策略0717是一个面向A股日线回测的模块化策略系统，当前重点是：

- RSI入场信号与哨兵价执行；
- 单股和多股共享同一交易核心；
- 网格加仓与账户资金约束；
- 回测过程可视化、逐笔成交回放和拒绝订单诊断；
- 策略收益与指数基准的实时对比。

当前代码仓库：`https://github.com/5792323-blip/-0717.git`

当前主要分支：`multi-stock-refactor-backup`

## 2. 当前已经完成

### 2.1 回测工作台

入口文件：`运行程序/interactive_backtest_app.py`

启动命令：

```bash
python3 -m 运行程序.interactive_backtest_app --host 127.0.0.1 --port 8501 --no-open
```

浏览器打开：`http://127.0.0.1:8501`

当前页面包含：

- 公共策略配置；
- 单股账户和多股共享账户配置；
- 股票池、日期、资金、最大持仓数和仓位上限；
- 首次开仓时机与网格加仓时机；
- 网格预算、网格模式和资金计划预览；
- 运行进度、停止回测和检查点入口；
- 拒绝订单原因汇总与逐笔拒绝明细；
- 最新报告、多股票回放和当前单票回放入口。

原来空白的结果卡“回测类型、样本数量、结果文件”已经移除，改成实时收益曲线区域。

### 2.2 策略模块

当前公共策略支持：

- RSI上穿20；
- RSI上穿30；
- RSI上穿均线；
- RSI上穿70；
- 反推价计算；
- 哨兵价形成；
- 哨兵价突破成交；
- 哨兵价突破后上移；
- 本根形成哨兵价后立即买入；
- 下一根K线执行；
- ATR跟踪止盈（仅盈利触发）；
- 网格加仓。

首次开仓和网格加仓可以分别选择：

- 本根形成哨兵价后立即买入；
- 下一根K线执行（预计算哨兵价·本根最高价触发）。

当前默认约定：

- 首次开仓：本根执行；
- 网格加仓：下一根K线执行。

### 2.3 资金与网格

资金相关设置分为三层：

1. 策略申请金额：策略想买多少；
2. 网格资金计划：首笔和后续加仓怎么分配；
3. 账户审批限制：实际最多允许成交多少。

常用参数含义：

| 参数 | 作用 |
|---|---|
| 资金基准金额 | 关闭网格时作为首笔金额；启用网格时作为网格预算基准 |
| 信号资金系数 | 只缩放该信号的普通首笔金额，不直接缩放网格指定股数 |
| 网格模式 | 固定分层或倍数加仓 |
| 单只股票累计仓位上限比例 | 单只股票最多占账户权益的比例 |
| 组合总持仓上限比例 | 所有股票持仓合计最多占账户权益的比例 |
| 最低现金保留比例 | 账户必须保留的现金比例 |
| 最大持仓数 | 组合最多持有多少只股票 |
| 允许部分成交 | 资金不足时缩减股数，但不改变价格和网格层级 |
| 买入流动性上限比例 | 页面保留该参数；当前交易核心暂时不使用成交额限制 |

多股共享账户的资金流：

```text
交易核心产生请求
        ↓
组合账户检查现金、单只上限、总仓位、最大持仓数
        ↓
允许全部成交 / 允许部分成交 / 拒绝
        ↓
记录实际成交或拒绝原因
```

### 2.4 拒绝订单诊断

主页进度区域提供：

- 拒绝订单总数；
- 拒绝原因汇总；
- 可展开的逐笔拒绝明细；
- 股票代码、类型、结果、原因、请求股数、成交股数、成交价和时间。

典型案例：600519曾出现“禁止部分成交”。当时请求300股，成交价约2000元，请求金额约60万元；单只结构性上限约36万元，而“允许部分成交”未勾选，因此整笔被拒绝。

处理方式：

- 想至少成交一部分：勾选“允许组合资金约束下部分成交”；
- 想让300股全部成交：提高“单只股票累计仓位上限比例”，通常需要约6%～7%；
- 不想提高单只仓位：降低网格层金额或指定股数。

### 2.5 停止回测

停止接口：`POST /api/backtest-stop`

已修复的问题：

- 最后一个时间点处理完成后不会继续误进入长时间整理流程；
- 整理多只股票回放文件期间会逐只检查停止请求；
- 页面会显示“正在停止”或“已停止（部分结果）”；
- 最近检查点可通过页面链接查看。

## 3. 实时收益曲线与基准指数

主页原结果卡区域现在显示：

`实时收益跟踪：策略 vs 指数`

曲线数据来自回测检查点，回测运行中按进度刷新，停止或完成后保留已经生成的曲线。

当前支持的指数基准：

- 沪深300；
- 中证500；
- 中证1000。

指数选择在左侧“多股账户与回测对象”中的“收益曲线对比指数”。它只影响收益曲线和超额收益计算，不改变股票池和交易规则。回测运行后该选择会锁定，避免同一次回测中途改变比较口径。

## 4. 指数数据

指数数据目录：`数据模块/大盘数据/`

当前文件：

- `hs300_日K线.pkl`：沪深300；
- `zz500_日K线.pkl`：中证500；
- `csi1000_日K线.pkl`：中证1000，已于2026-07-28补充。

中证1000数据来源：baostock，指数代码`sh.000852`。

中证1000本地数据范围：2005-01-04至2026-07-27，共4993条日线，字段为`date/open/high/low/close/amount`。

说明：项目的`.gitignore`默认忽略大多数`*.pkl`历史数据；中证1000文件已显式加入Git，以便基准功能在GitHub副本中可用。完整行情数据仍建议保留电脑或外部硬盘备份。

## 5. 主要代码位置

| 功能 | 文件 |
|---|---|
| 回测主页、参数表单、进度API | `运行程序/interactive_backtest_app.py` |
| 多股共享账户时间轴回测 | `组合回测/统一多股执行器.py` |
| 单股/多股交易核心 | `策略引擎/规则执行器.py` |
| 账户现金和仓位审批 | `策略引擎/交易账户.py` |
| 单股回放模板 | `回测引擎/kline_template.html` |
| 策略参数 | `1_策略配置/` |
| 买入规则 | `5_买入规则/` |
| 卖出规则 | `6_卖出规则/` |
| 仓位管理 | `7_仓位管理/` |
| 过滤因子 | `8_过滤因子/` |
| 自动生成的实验结果 | `10_实验记录/` |
| 设计和验证文档 | `11_策略文档/` |

## 6. 验证方式

修改Python代码后至少执行：

```bash
python3 -m py_compile 运行程序/interactive_backtest_app.py
python3 -m py_compile 组合回测/统一多股执行器.py
git diff --check
```

页面启动后检查：

```bash
curl http://127.0.0.1:8501/api/backtest-progress
```

正常空闲状态应返回`status: idle`。回测运行时应返回`status: running`，停止后应返回`status: stopped`或最终状态。

## 7. 当前已知事项

- 大量历史行情和实验产物可能占用较多磁盘空间；当前策略是每次新回测前清理受控的旧交互回测目录。
- GitHub仓库不等同于完整数据备份；完整项目目录目前约25GB。
- 主页顶部“回测视图”按钮仍用于在参数回测台、最新报告、多股票回放和当前单票回放之间切换。
- 当前收益曲线使用检查点中的采样点，不是每一根K线都写入页面；这是为了避免回测期间页面数据过大。
- 指数基准是回测开始前确定的，同一次回测运行中不能切换。
- 当前部分策略配置仍保留兼容旧版本的字段，页面中隐藏了部分旧选项，但底层迁移逻辑仍存在。

## 8. 后续建议

1. 继续简化主页导航，删除空的“当前股票”提示和重复标题。
2. 将指数数据下载和更新整理成一个可重复运行的脚本，并记录数据更新时间。
3. 让拒绝订单明细支持按股票、原因和日期筛选。
4. 为实时收益曲线增加鼠标悬浮日期和收益值提示。
5. 对中证1000股票池是否需要历史时点成分股数据进行单独评估，避免把当前成分快照当成历史成分。

## 9. 最近重要提交

- `282eac3`：增加实时收益曲线与多指数基准；补充中证1000日线数据。
- `d02c915`：修复回测停止后的收尾流程。
- `47b498a`：优化回测主页与拒绝订单明细。
- `99df3e0`：同步策略0717当前代码。
- `a89df70`：完善回测因子与网格加仓流程。

以后继续开发时，先阅读本文件，再查看最新Git提交和`11_策略文档/`目录。
