# 策略0717项目进度说明

最后更新：2026-07-28

## 1. 项目定位

策略0717是一个面向A股日线回测的模块化策略系统，当前重点是：

- RSI入场信号与哨兵价执行；
- 单股和多股共享同一交易核心；
- 网格加仓与账户资金约束；
- 回测过程可视化、逐笔成交回放和拒绝订单诊断；
- 策略收益与指数基准的实时对比。

当前代码仓库：`https://github.com/5792323-blip/-0717.git`

当前主要分支：`multi-stock-refactor-backup`

## 2. 当前已经完成

### 2.1 回测工作台

入口文件：`运行程序/interactive_backtest_app.py`

启动命令：

```bash
python3 -m 运行程序.interactive_backtest_app --host 127.0.0.1 --port 8501 --no-open
```

浏览器打开：`http://127.0.0.1:8501`

当前页面包含：

- 公共策略配置；
- 单股账户和多股共享账户配置；
- 股票池、日期、资金、最大持仓数和仓位上限；
- 首次开仓时机与网格加仓时机；
- 网格预算、网格模式和资金计划预览；
- 运行进度、停止回测和检查点入口；
- 拒绝订单原因汇总与逐笔拒绝明细；
- 最新报告、多股票回放和当前单票回放入口。

原来空白的结果卡“回测类型、样本数量、结果文件”已经移除，改成实时收益曲线区域。

### 2.2 策略模块

当前公共策略支持：

- RSI上穿20；
- RSI上穿30；
- RSI上穿均线；
- RSI上穿70；
- 反推价计算；
- 哨兵价形成；
- 哨兵价突破成交；
- 哨兵价突破后上移；
- 本根形成哨兵价后立即买入；
- 下一根K线执行；
- ATR跟踪止盈（仅盈利触发）；
- 网格加仓。

首次开仓和网格加仓可以分别选择：

- 本根形成哨兵价后立即买入；
- 下一根K线执行（预计算哨兵价·本根最高价触发）。

当前默认约定：

- 首次开仓：本根执行；
- 网格加仓：下一根K线执行。

哨兵价口径（2026-07-28确认）：

- 哨兵价是信号层记录：每次有效的RSI上穿20、30、RSI-MA或70，都会形成或更新哨兵价并显示在K线回放中；同类型的旧待成交哨兵不会抑制新的上穿事件。
- RSI-MA上穿按标准两点比较：上一根`RSI ≤ RSI-MA`且本根`RSI > RSI-MA`。
- 过滤因子、账户资金、仓位上限和成交条件只决定后续是否成交，不会取消已形成的哨兵价。

### 2.3 资金与网格

资金相关设置分为三层：

1. 策略申请金额：策略想买多少；
2. 网格资金计划：首笔和后续加仓怎么分配；
3. 账户审批限制：实际最多允许成交多少。

常用参数含义：

| 参数 | 作用 |
|---|---|
| 资金基准金额 | 关闭网格时作为首笔金额；启用网格时作为网格预算基准 |
| 信号资金系数 | 只缩放该信号的普通首笔金额，不直接缩放网格指定股数 |
| 网格模式 | 默认线性加仓：L0=1、L1=2、L2=3、L3=4、L4=5；可选倍数加仓：L0=1、L1=2、L2=4、L3=8、L4=16 |
| 单只股票累计仓位上限比例 | 单只股票最多占账户权益的比例 |
| 组合总持仓上限比例 | 所有股票持仓合计最多占账户权益的比例 |
| 最低现金保留比例 | 账户必须保留的现金比例 |
| 最大持仓数 | 组合最多持有多少只股票 |
| 允许部分成交 | 资金不足时缩减股数，但不改变价格和网格层级 |
| 买入流动性上限比例 | 页面保留该参数；当前交易核心暂时不使用成交额限制 |

已确认的网格交易口径（2026-07-28）：

- RSI上穿20、30、均线或70的资金系数，只确定首次开仓金额；不会传递或缩放后续网格股数。
- 网格回撤达到层级后仅形成待确认订单；必须再次出现新的哨兵价上穿与有效买入信号，才可成交。
- 以首笔实际成交200股为例，L1/L2/L3/L4分别为400/600/800/1,000股；若L1和L2同时待确认，合并请求1,000股，再由账户资金约束决定是否全额、部分或拒绝成交。
- 单股和多股共用上述交易核心与网格数量规则。页面默认模式已切换为“线性加仓：L0=1 / L1=2 / L2=3 / L3=4 / L4=5”；旧的`multiplier`配置只保留读取兼容，不作为新回测默认值。

多股共享账户的资金流：

```text
交易核心产生请求
        ↓
组合账户检查现金、单只上限、总仓位、最大持仓数
        ↓
允许全部成交 / 允许部分成交 / 拒绝
        ↓
记录实际成交或拒绝原因
```

当前工作台多股账户口径（2026-07-28用户确认）：

- 最大持仓数、单只仓位上限和总仓位上限均设为`0`，表示不限；页面和引擎会明确显示“仅受现金约束”。
- 已开启部分成交：当现金不足完整请求但足以买入交易所最小单位时，按可用现金缩减成交，不再以“禁止部分成交”整笔拒绝。
- 始终保留真实现金余额、最低现金保留比例（当前为0）和交易所最小交易单位约束；系统不允许透支现金。

### 2.4 拒绝订单诊断

主页进度区域提供：

- 拒绝订单总数；
- 拒绝原因汇总；
- 可展开的逐笔拒绝明细；
- 股票代码、类型、结果、原因、请求股数、成交股数、成交价和时间。

典型案例：600519曾出现“禁止部分成交”。当时请求300股，成交价约2000元，请求金额约60万元；单只结构性上限约36万元，而“允许部分成交”未勾选，因此整笔被拒绝。

处理方式：

- 想至少成交一部分：勾选“允许组合资金约束下部分成交”；
- 想让300股全部成交：提高“单只股票累计仓位上限比例”，通常需要约6%～7%；
- 不想提高单只仓位：降低网格层金额或指定股数。

### 2.5 停止回测

停止接口：`POST /api/backtest-stop`

已修复的问题：

- 最后一个时间点处理完成后不会继续误进入长时间整理流程；
- 整理多只股票回放文件期间会逐只检查停止请求；
- 页面会显示“正在停止”或“已停止（部分结果）”；
- 最近检查点可通过页面链接查看。

### 2.6 严格历史股票池与行业景气回测准备

- 沪深300历史成分股已接入：回测在每个交易时点仅允许当时成分股新开仓；成分调整后已有持仓仍按原卖出、止损与网格规则管理。
- 新增“行业景气历史准入”过滤器：只拦截新开仓，不改变交易执行、网格加仓或卖出逻辑；默认关闭，工作台状态为“未就绪”。
- 过滤器要求同时具备当时已经生效的个股行业归属和行业景气评分。任一记录缺失即拦截，不会使用当前行业分类或事后评分回填历史。
- 已建立行业景气历史数据格式与模板：`基本面/历史数据/`。
- 已下载申万历史个股行业变动原始记录：`基本面/历史数据/申万股票行业变动_raw.csv`，共12,882条，涉及553个行业代码。
- 已生成2014年以来的申万代码到工作簿31个一级行业的保守映射：452个代码中451个可归并；唯一未归并的`210301`（其他采掘）保留缺失，不猜测行业归属。
- 已生成可用于严格回测的`基本面/历史数据/股票行业归属历史.csv`：8,957条记录、5,445只股票、31个一级行业。`210301`对应记录被排除；过滤器遇到该类股票会因历史归属缺失而阻止新开仓。
- 行业评分历史序列尚未补齐，因此当前不能启动有效的“行业评分选行业”严格回测。
- 已导出`基本面/历史数据/行业历史指标采集清单.csv`：31个一级行业共341项指标，其中126项核心先行指标（P0）应优先补齐；历史评分构建器默认要求单行业90%权重覆盖。
- 已完成P0数据源就绪审计：126项中48项可从公开官方源建设历史抓取，19项需要第三方/公司数据授权或人工导入，59项仍待逐源核验；审计表为`基本面/历史数据/行业历史数据源就绪审计.csv`。
- 历史评分采用“公开官方数据优先、确认数据可覆盖”策略：同一指标同一生效日按来源优先级选取，后续确认的Wind/Choice/iFinD/Tushare等导出可替换公开初版，不影响其他历史时点。
- 公开宏观序列已验证日期语义：制造业PMI可按国家统计局发布日期使用；AKShare的M2日期与央行实际发布日期不一致，现仅保留原始观测，不得写入历史评分或回测。
- 已增加历史评分质量审计：`基本面/审计行业景气历史评分.py`会检查字段、前视日期、90%覆盖权重和行业覆盖。当前审计结果为未就绪（仅有PMI单指标评分，尚无满足覆盖门槛的行业总评分），因此严格A/B回测不会启动。
- 已增加 v3 当前台账审计：`基本面/审计行业景气当前台账.py`。2026-07-28 检查结果为 525 个指标有当前值和当前评分，但 0 个有数据发布日期，且 525 个均为“待更新”；当前评分不能直接用于严格回测。
- 已增加个股与行业联合准入模块：`8_过滤因子/fundamental_industry_combo_filter.py`，采用个股基本面60% + 行业景气40%。联合模块默认关闭，需待行业历史评分和个股评分数据通过审计后再启用。
- 已增加一键联合评分管线：`基本面/联合评分管线.py`。管线会审计 v3 台账、个股快照、行业历史评分并决定联合模块是否可启用；当前运行结果已落盘到 `基本面/运行记录/联合评分管线报告.json`，结论为未就绪，不会用当前值回填历史。

## 3. 实时收益曲线与基准指数

主页原结果卡区域现在显示：

`实时收益跟踪：策略 vs 指数`

曲线数据来自回测检查点，回测运行中按进度刷新，停止或完成后保留已经生成的曲线。

当前支持的指数基准：

- 沪深300；
- 中证500；
- 中证1000。

指数选择在左侧“多股账户与回测对象”中的“收益曲线对比指数”。它只影响收益曲线和超额收益计算，不改变股票池和交易规则。回测运行后该选择会锁定，避免同一次回测中途改变比较口径。

### 3.1 最近补强

- 已加固回测输出断管处理：页面断开或终端管道中断时，不再把 `BrokenPipeError` 误判成策略失败。
- `interactive_backtest_app.py` 和 `组合回测/统一多股执行器.py` 都已做安全封装，回测收尾会优先落盘结果。
- 当前更适合把 `BrokenPipeError` 视为“输出连接断开”而不是“交易逻辑异常”；若最终结果文件已生成，策略本身通常仍已跑完。

## 4. 指数数据

指数数据目录：`数据模块/大盘数据/`

当前文件：

- `hs300_日K线.pkl`：沪深300；
- `zz500_日K线.pkl`：中证500；
- `csi1000_日K线.pkl`：中证1000，已于2026-07-28补充。

中证1000数据来源：baostock，指数代码`sh.000852`。

中证1000本地数据范围：2005-01-04至2026-07-27，共4993条日线，字段为`date/open/high/low/close/amount`。

说明：项目的`.gitignore`默认忽略大多数`*.pkl`历史数据；中证1000文件已显式加入Git，以便基准功能在GitHub副本中可用。完整行情数据仍建议保留电脑或外部硬盘备份。

## 5. 主要代码位置

| 功能 | 文件 |
|---|---|
| 回测主页、参数表单、进度API | `运行程序/interactive_backtest_app.py` |
| 多股共享账户时间轴回测 | `组合回测/统一多股执行器.py` |
| 单股/多股交易核心 | `策略引擎/规则执行器.py` |
| 账户现金和仓位审批 | `策略引擎/交易账户.py` |
| 单股回放模板 | `回测引擎/kline_template.html` |
| 策略参数 | `1_策略配置/` |
| 买入规则 | `5_买入规则/` |
| 卖出规则 | `6_卖出规则/` |
| 仓位管理 | `7_仓位管理/` |
| 过滤因子 | `8_过滤因子/` |
| 行业景气体系、公开数据与历史数据模板 | `基本面/` |
| 自动生成的实验结果 | `10_实验记录/` |
| 设计和验证文档 | `11_策略文档/` |

## 6. 验证方式

修改Python代码后至少执行：

```bash
python3 -m py_compile 运行程序/interactive_backtest_app.py
python3 -m py_compile 组合回测/统一多股执行器.py
git diff --check
```

页面启动后检查：

```bash
curl http://127.0.0.1:8501/api/backtest-progress
```

正常空闲状态应返回`status: idle`。回测运行时应返回`status: running`，停止后应返回`status: stopped`或最终状态。

## 7. 当前已知事项

### 7.1 当前基本面与行业景气数据接入（2026-07-28）

- 已用 AKShare 公开接口更新固定 288 只股票的当前财务快照，成功 288、失败 0。
- 个股快照新增当前估值 `PE_TTM`、`PB_MRQ`、估值日期，以及利息保障倍数、有息负债率等财务健康指标。
- 当前估值属于评分日附近的市场快照，不回填到历史回测；严格历史评分仍要求估值在当时已可获得。
- 个股评分新增“数据不足”状态。当前平均置信度约 0.28，286 只股票因覆盖率不足标为“数据不足”，不是明确的基本面否决。
- 行业 v3 工作簿仍有 525 个指标缺少实际发布日期，行业历史评分文件为空，个股与行业联合准入继续关闭。
- 联合管线已重新执行并写入 `基本面/运行记录/联合评分管线报告.json`；不会使用当前行业值伪造历史评分。
- 已增加 `基本面/个股研究排序.csv` 和 `基本面/运行记录/个股覆盖率审计.json`：前者用于当前人工筛选，后者显示各维度覆盖率；两者均不会开启严格历史准入。
- 已增加 `基本面/基本面研究池.csv` 和 `基本面/运行记录/基本面研究池报告.json`：先排除硬风险，再按总分和置信度选出最多50只研究标的；研究池不拦截正式回测。
- 研究池口径已修正：明确事件风险才硬排除；单期 ROE/ROIC 或经营现金流异常只作为“复核提醒”，避免因单期数据误删周期股。
- 已新增“基本面评分准入”过滤因子：默认关闭，可在主页“过滤因子”中开启；默认最低总分65、最低置信度0.25，只影响首次开仓，已有持仓、卖出和网格加仓不受影响。当前评分只有2026-07-28快照，开启后不能直接回测更早历史日期。
- 已新增个股历史基本面完整性审计：`基本面/审计个股历史基本面.py`；要求真实披露时点、至少4个历史评分日、288只股票和关键指标90%覆盖。当前审计未通过，基本面历史回测会被阻止。

### 7.2 个股历史基本面下载结果（2026-07-29）

- 已生成独立历史文件 `数据模块/基本面历史快照_2020_2026.csv`，未覆盖当前快照；固定 288 只股票全部成功，6363 条记录，失败 0。
- 财务报告期覆盖 2020-03-31 至 2026-06-30；实际公告日覆盖 99.7%，所有已填写的公告日均不晚于评分日，且股票与公告日没有重复记录。
- 当前文件可用于下一步生成“按实际公告日生效”的历史评分，但还不能宣称基本面评分已完整覆盖 2020-2026。
- 严格审计未通过：PE_TTM、PB_MRQ、EV_EBITDA、FCF_Yield 历史值尚未补齐；ROIC、扣非净利润3年CAGR、有息负债率、利息保障倍数等部分指标覆盖不足，另有 22 条记录缺少实际公告日。
- 因此基本面评分过滤器继续默认关闭，不能把当前快照值回填到历史日期，也不能在缺字段时用 0 或当前值伪造历史数据。

### 7.3 市场状态演化因子（2026-07-29）

- 已新增七个独立扩展因子：CMSF市场状态（MS）、状态转换率（CTR）、状态持续期（CST）、峰值距离（CPD）、价格状态衰减（CDF）、状态混乱度（CDE）和顶部风险（CTRS）。
- 所有模块默认关闭，新增后不会改变当前买入、哨兵价、网格、成交或卖出逻辑。
- CMSF 每根K线只记录状态；买入侧只读取上一根完成状态，作为已形成哨兵价的可选过滤器，不会生成买入订单或改变哨兵价。
- 卖出侧不生成卖出订单，只能对已经由原卖出规则触发的信号进行可选放行/暂缓；默认没有阈值，因此不拦截任何卖出。保护性卖出规则可配置为永不拦截。
- 当前版本为研究/观察模块；必须先做样本外分组验证（TopRisk 分位与未来回撤、ATR 卖出、趋势利润的关系），再考虑启用任何过滤阈值。

### 7.4 市场状态哨兵过滤实验（2026-07-29）

- 新增独立的买入端因子：压缩完成度 CCR、状态反转 CRI、稳定吸引度 CSA；它们只能过滤已触达的原哨兵价，不能改写哨兵价或生成订单。
- 买入端 CMSF 默认只过滤首次哨兵开仓，已有持仓后的网格加仓严格保持原逻辑。
- 以 5 只股票在 2020-2022 年做可交易性诊断后，用固定宽阈值在 2023-2025 年验证：买入过滤将交易数由 1,019 降为 208，胜率由 35.04% 提高到 42.31%，平均最大回撤由 0.2708% 降至 0.1058%，但平均年化收益接近零，尚不能确认绝对收益有效。
- 卖出端 `CTRS>=60` 放行普通卖出的实验产生 27 笔买入、0 笔卖出并显著恶化回撤，已判定无效；默认配置没有启用此阈值。
- 完整实验方法、参数、逐股结果和文件索引见 `11_策略文档/2026-07-29_CMSF哨兵状态过滤实验.md`；实验 JSON 位于 `10_实验记录/CMSF哨兵过滤_20260729/`。

- 大量历史行情和实验产物可能占用较多磁盘空间；当前策略是每次新回测前清理受控的旧交互回测目录。
- GitHub仓库不等同于完整数据备份；完整项目目录目前约25GB。
- 主页顶部“回测视图”按钮仍用于在参数回测台、最新报告、多股票回放和当前单票回放之间切换。
- 当前收益曲线使用检查点中的采样点，不是每一根K线都写入页面；这是为了避免回测期间页面数据过大。
- 指数基准是回测开始前确定的，同一次回测运行中不能切换。
- `BrokenPipeError` 现在已按断管中断处理，但如果浏览器页签频繁关闭或刷新，仍可能让当前任务提前停止刷新进度。
- 当前部分策略配置仍保留兼容旧版本的字段，页面中隐藏了部分旧选项，但底层迁移逻辑仍存在。
- 行业景气工作簿的原始值目前仍以2026年5月快照为主；公开数据自动更新有时效保护，数据源滞后时不会覆盖原值或改写人工评分。
- 申万历史行业变动原始数据仅含行业代码；在代码映射和历史评分均完整前，行业景气过滤器必须保持关闭。

## 8. 后续建议

1. 继续简化主页导航，删除空的“当前股票”提示和重复标题。
2. 将指数数据下载和更新整理成一个可重复运行的脚本，并记录数据更新时间。
3. 让拒绝订单明细支持按股票、原因和日期筛选。
4. 为实时收益曲线增加鼠标悬浮日期和收益值提示。
5. 对中证1000股票池是否需要历史时点成分股数据进行单独评估，避免把当前成分快照当成历史成分。
6. 以权威申万分类版本对照补全553个历史行业代码，再生成`股票行业归属历史.csv`并做时点抽样核验。
7. 按实际发布日期补齐月度/季频行业景气评分历史表；完成后以“行业评分过滤关闭/开启”做严格对照回测。

## 9. 最近重要提交

- `282eac3`：增加实时收益曲线与多指数基准；补充中证1000日线数据。
- `bb5354a`：加固回测输出断管处理。
- `d02c915`：修复回测停止后的收尾流程。
- `47b498a`：优化回测主页与拒绝订单明细。
- `99df3e0`：同步策略0717当前代码。
- `a89df70`：完善回测因子与网格加仓流程。

以后继续开发时，先阅读本文件，再查看最新Git提交和`11_策略文档/`目录。
